# Phase 2 — ja / en 雙語接入 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 30 篇 `index.ja.md` + 30 篇 `index.en.md` 翻譯接進 Astro，提供 `/ja/`、`/en/` 路由與跨語言切換 UI；缺翻譯回 404，與 Next.js prod 一致。

**Architecture:** 延續單一 `posts` Content Collection。Glob 收回 `index.{ja,en}.md`；`lib/posts.ts` 從檔名 suffix 推 locale，新增 `availableLocales` 聚合欄位；新增 `src/lib/i18n.ts` 集中常數與 helper；新增三個 UI 元件（LanguageSwitcher、ArticleLanguageIndicator、LanguageNotice）；所有 `[locale]/*` 頁面 `getStaticPaths` 改為 `LOCALES.map(...)`，archives 例外只產 zh。

**Tech Stack:** Astro 6.x、TypeScript、vitest、Astro Content Collections (glob loader)、Astro `<Image>` / Font API（前置 phase 已就緒）。

**Spec：** `docs/superpowers/specs/2026-05-01-phase-2-i18n-design.md`

**所有路徑相對於 `astro/` 子目錄（除非另註明）。** 工作目錄請固定在 `astro/`。

---

## 檔案結構

**Create:**
- `src/lib/i18n.ts` — locale 列表、HTML lang、UI 文字、URL helper
- `src/lib/__tests__/i18n.test.ts` — `localePath` 測試
- `src/lib/__tests__/posts.test.ts` — locale 推斷、`availableLocales` 聚合、slug 唯一性測試
- `src/components/LanguageSwitcher.astro` — 語言切換 dropdown
- `src/components/ArticleLanguageIndicator.astro` — 文章頁顯示「Also available in: X」
- `src/components/LanguageNotice.astro` — ja/en 首頁中文站提示
- `src/lib/__tests__/language-switcher-links.test.ts` — `buildLanguageLinks` 純函式測試

**Modify:**
- `src/content.config.ts` — 移除 glob 對 `index.{ja,en}.md` 的排除
- `src/lib/posts.ts` — locale 推斷、`availableLocales`、slug uniqueness 改 (locale, slug) 對、API 加 locale 參數
- `src/pages/[locale]/index.astro` — `getStaticPaths` 改 `LOCALES.map`、傳 locale 到 `getActivePosts`、加 LanguageNotice、`Astro.params.locale!` 收斂
- `src/pages/[locale]/tech.astro` — 同上
- `src/pages/[locale]/life.astro` — 同上
- `src/pages/[locale]/about.astro` — `getStaticPaths` 改 `LOCALES.map`、`Astro.params.locale!` 收斂
- `src/pages/[locale]/subscription.astro` — 同 about
- `src/pages/[locale]/archives.astro` — 維持只產 zh，確認 import 與 helper 仍正確
- `src/pages/[locale]/posts/[slug].astro` — `getStaticPaths` 由 `getAllPosts()` 直接展開 (locale, slug)
- `src/components/Header.astro` — 改用 `t(locale)`、ja/en 隱藏 Archives、嵌入 LanguageSwitcher、新增 props
- `src/components/PostList.astro` — 連結走 `localePath()`
- `src/layouts/PostLayout.astro` — 嵌入 `ArticleLanguageIndicator`、產出 hreflang alternates、傳 `availableLocales` 給 Header

---

## Task 1：建立 `src/lib/i18n.ts`

**Files:**
- Create: `src/lib/i18n.ts`
- Create: `src/lib/__tests__/i18n.test.ts`

- [ ] **Step 1: 寫失敗測試 `i18n.test.ts`**

```ts
// src/lib/__tests__/i18n.test.ts
import { describe, it, expect } from 'vitest';
import { localePath, LOCALES, HTML_LANG, HREFLANG, LANGUAGE_NAMES, t } from '../i18n';

describe('LOCALES', () => {
  it('contains zh, ja, en in this order', () => {
    expect(LOCALES).toEqual(['zh', 'ja', 'en']);
  });
});

describe('localePath', () => {
  it('builds locale-only path when no segments', () => {
    expect(localePath('zh')).toBe('/zh');
  });

  it('joins segments with single slash', () => {
    expect(localePath('ja', 'tech')).toBe('/ja/tech');
    expect(localePath('en', 'posts', '2024-foo')).toBe('/en/posts/2024-foo');
  });

  it('drops empty segments', () => {
    expect(localePath('zh', '', 'tech')).toBe('/zh/tech');
  });
});

describe('HTML_LANG / HREFLANG / LANGUAGE_NAMES', () => {
  it('maps every locale', () => {
    expect(HTML_LANG.zh).toBe('zh-Hant-TW');
    expect(HTML_LANG.ja).toBe('ja');
    expect(HTML_LANG.en).toBe('en');
    expect(HREFLANG.zh).toBe('zh-Hant');
    expect(LANGUAGE_NAMES.zh).toBe('繁體中文');
    expect(LANGUAGE_NAMES.ja).toBe('日本語');
    expect(LANGUAGE_NAMES.en).toBe('English');
  });
});

describe('t(locale)', () => {
  it('returns nav strings per locale', () => {
    expect(t('zh').nav.tech).toBe('技術');
    expect(t('ja').nav.tech).toBe('技術');
    expect(t('en').nav.tech).toBe('Tech');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/i18n.test.ts
```

Expected: FAIL with "Cannot find module '../i18n'"

- [ ] **Step 3: 實作 `src/lib/i18n.ts`**

```ts
// src/lib/i18n.ts
export const LOCALES = ['zh', 'ja', 'en'] as const;
export type Locale = (typeof LOCALES)[number];

export const HTML_LANG: Record<Locale, string> = {
  zh: 'zh-Hant-TW',
  ja: 'ja',
  en: 'en',
};

export const HREFLANG: Record<Locale, string> = {
  zh: 'zh-Hant',
  ja: 'ja',
  en: 'en',
};

export const LANGUAGE_NAMES: Record<Locale, string> = {
  zh: '繁體中文',
  ja: '日本語',
  en: 'English',
};

export function localePath(locale: Locale, ...segments: string[]): string {
  const parts = [locale, ...segments].filter((s) => s !== '' && s !== undefined);
  return '/' + parts.join('/');
}

export interface UiText {
  nav: {
    home: string;
    tech: string;
    life: string;
    archives: string;
    about: string;
    subscription: string;
  };
  post: {
    alsoAvailableIn: string;
  };
  languageNotice: {
    mainlyInChinese: string;
    currentlyHas: string;
    articles: string;
    switchToChinese: string;
  };
  switchLanguage: string;
}

export const UI_TEXT: Record<Locale, UiText> = {
  zh: {
    nav: { home: '首頁', tech: '技術', life: '生活', archives: '歸檔', about: '關於', subscription: '訂閱' },
    post: { alsoAvailableIn: '其他語言版本：' },
    languageNotice: {
      mainlyInChinese: '本站主要以繁體中文撰寫',
      currentlyHas: '目前有',
      articles: '篇文章',
      switchToChinese: '切換到中文版以瀏覽所有內容',
    },
    switchLanguage: '切換語言',
  },
  ja: {
    nav: { home: 'ホーム', tech: '技術', life: '生活', archives: 'アーカイブ', about: '概要', subscription: '購読' },
    post: { alsoAvailableIn: 'Also available in:' },
    languageNotice: {
      mainlyInChinese: 'このサイトは主に繁体字中国語で書かれています',
      currentlyHas: '現在',
      articles: '件の記事があります',
      switchToChinese: '中国語版に切り替えるとすべてのコンテンツをご覧いただけます',
    },
    switchLanguage: '言語切替',
  },
  en: {
    nav: { home: 'Home', tech: 'Tech', life: 'Life', archives: 'Archives', about: 'About', subscription: 'Subscribe' },
    post: { alsoAvailableIn: 'Also available in:' },
    languageNotice: {
      mainlyInChinese: 'This site is mainly written in Traditional Chinese',
      currentlyHas: 'Currently there are',
      articles: 'articles',
      switchToChinese: 'Switch to the Chinese version to browse all content',
    },
    switchLanguage: 'Switch language',
  },
};

export function t(locale: Locale): UiText {
  return UI_TEXT[locale];
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npx vitest run src/lib/__tests__/i18n.test.ts
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n.ts src/lib/__tests__/i18n.test.ts
git commit -m "feat(astro): add i18n helper module"
```

---

## Task 2：在 `lib/posts.ts` 推 locale + `availableLocales`

**Files:**
- Modify: `src/lib/posts.ts`
- Create: `src/lib/__tests__/posts.test.ts`

本 task 只新增 locale 推斷、`availableLocales` 計算、slug uniqueness 改成 (locale, slug) 對。**不改 API 簽章**（getActivePosts、getPostsByCategory 還沒有 locale 參數，下個 task 處理）。本 task 結束時 build 仍只產 zh，因為 glob 還沒收 ja/en。

- [ ] **Step 1: 寫失敗測試 `posts.test.ts`**

```ts
// src/lib/__tests__/posts.test.ts
import { describe, it, expect } from 'vitest';
import { inferLocaleFromFilename, computeAvailableLocales } from '../posts';
import type { Locale } from '../i18n';

describe('inferLocaleFromFilename', () => {
  it('returns ja for index.ja', () => {
    expect(inferLocaleFromFilename('index.ja')).toBe('ja');
  });
  it('returns en for index.en', () => {
    expect(inferLocaleFromFilename('index.en')).toBe('en');
  });
  it('returns zh for plain index', () => {
    expect(inferLocaleFromFilename('index')).toBe('zh');
  });
  it('returns zh for any other filename', () => {
    expect(inferLocaleFromFilename('2025-01-01_foo')).toBe('zh');
    expect(inferLocaleFromFilename('我的文章')).toBe('zh');
  });
});

describe('computeAvailableLocales', () => {
  type Row = { group: string; dirname: string; locale: Locale };

  it('returns single zh for posts with no translations', () => {
    const rows: Row[] = [{ group: '2024', dirname: '2024-01-01_a', locale: 'zh' }];
    const map = computeAvailableLocales(rows);
    expect(map.get('2024::2024-01-01_a')).toEqual(['zh']);
  });

  it('aggregates locales by (group, dirname) and sorts as zh,ja,en', () => {
    const rows: Row[] = [
      { group: '2024', dirname: '2024-02-02_b', locale: 'en' },
      { group: '2024', dirname: '2024-02-02_b', locale: 'ja' },
      { group: '2024', dirname: '2024-02-02_b', locale: 'zh' },
    ];
    const map = computeAvailableLocales(rows);
    expect(map.get('2024::2024-02-02_b')).toEqual(['zh', 'ja', 'en']);
  });

  it('handles orphan translation (ja-only, no zh)', () => {
    const rows: Row[] = [
      { group: '2024', dirname: '2024-03-03_c', locale: 'ja' },
    ];
    const map = computeAvailableLocales(rows);
    expect(map.get('2024::2024-03-03_c')).toEqual(['ja']);
  });

  it('does not cross-pollute different dirnames', () => {
    const rows: Row[] = [
      { group: '2024', dirname: 'a', locale: 'zh' },
      { group: '2024', dirname: 'b', locale: 'ja' },
    ];
    const map = computeAvailableLocales(rows);
    expect(map.get('2024::a')).toEqual(['zh']);
    expect(map.get('2024::b')).toEqual(['ja']);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/posts.test.ts
```

Expected: FAIL（function 尚未 export）

- [ ] **Step 3: 修改 `src/lib/posts.ts`**

在檔案頂部加 import：
```ts
import { LOCALES, type Locale } from './i18n';
```

修改 `PostMeta` interface（檔案開頭附近）：
```ts
export interface PostMeta {
  entry: PostEntry;
  slug: string;
  group: string;
  archived: boolean;
  category: 'tech' | 'life';
  title: string;
  date: Date;
  description?: string;
  cover: ImageMetadata | null;
  locale: Locale;
  availableLocales: Locale[];
}
```

加入兩個 helper（放在 `parsePathSegments` 上方）：
```ts
/**
 * Infer post locale from the source filename (without extension).
 * `index.ja` -> ja, `index.en` -> en, anything else -> zh.
 */
export function inferLocaleFromFilename(filename: string): Locale {
  if (filename === 'index.ja') return 'ja';
  if (filename === 'index.en') return 'en';
  return 'zh';
}

/**
 * Group rows by `${group}::${dirname}` and produce the sorted locale list per group.
 * Locales are sorted to follow LOCALES order (zh, ja, en) so the output is stable.
 */
export function computeAvailableLocales(
  rows: { group: string; dirname: string; locale: Locale }[],
): Map<string, Locale[]> {
  const sets = new Map<string, Set<Locale>>();
  for (const row of rows) {
    const key = `${row.group}::${row.dirname}`;
    let set = sets.get(key);
    if (!set) {
      set = new Set();
      sets.set(key, set);
    }
    set.add(row.locale);
  }
  const result = new Map<string, Locale[]>();
  for (const [key, set] of sets) {
    result.set(key, LOCALES.filter((l) => set.has(l)));
  }
  return result;
}
```

修改 `toMeta()`：在 `parsed = parsePathSegments(entry)` 之後新增：
```ts
const locale = inferLocaleFromFilename(filename);
```

並把 `locale` 與 `availableLocales`（先暫填 `[locale]`，下一步從 `getAllPosts()` 反塞）放進 return：
```ts
return {
  entry,
  slug,
  group,
  archived: group === 'archives',
  category: resolveCategory(entry.data),
  title,
  date,
  description: entry.data.description,
  cover,
  locale,
  availableLocales: [locale],  // placeholder; getAllPosts overwrites
};
```

修改 `getAllPosts()` 將 `availableLocales` 反塞、且把 slug uniqueness 改成 (locale, slug) 對：
```ts
export async function getAllPosts(): Promise<PostMeta[]> {
  const entries = await getCollection('posts');
  const resolved = await Promise.all(entries.map(toMeta));
  const sorted = resolved
    .filter((p): p is PostMeta => p !== null)
    .sort((a, b) => b.date.getTime() - a.date.getTime());

  // Compute availableLocales by (group, dirname).
  const rows = sorted.map((p) => ({
    group: p.group,
    dirname: dirnameFromEntry(p.entry),
    locale: p.locale,
  }));
  const localeMap = computeAvailableLocales(rows);
  for (const p of sorted) {
    const key = `${p.group}::${dirnameFromEntry(p.entry)}`;
    p.availableLocales = localeMap.get(key) ?? [p.locale];
  }

  // Slug uniqueness assertion: (locale, slug) pairs must be unique.
  const seen = new Map<string, string>();
  for (const post of sorted) {
    const key = `${post.locale}::${post.slug}`;
    const prev = seen.get(key);
    if (prev) {
      throw new Error(
        `[posts] Duplicate (locale=${post.locale}, slug="${post.slug}") in entries: ${prev} and ${post.entry.id}`,
      );
    }
    seen.set(key, post.entry.id);
  }

  return sorted;
}
```

新增 helper（放在 `parsePathSegments` 下方）：
```ts
/**
 * Re-derive dirname from an entry, used for grouping translations of the same post.
 */
function dirnameFromEntry(entry: PostEntry): string {
  return parsePathSegments(entry)?.dirname ?? '';
}
```

- [ ] **Step 4: 跑單元測試確認通過**

```bash
npx vitest run src/lib/__tests__/posts.test.ts
```

Expected: PASS

- [ ] **Step 5: 跑全部測試確認沒打破其它**

```bash
npx vitest run
```

Expected: PASS（既有 26 個 + 新 8 個 = 34 個）

- [ ] **Step 6: Commit**

```bash
git add src/lib/posts.ts src/lib/__tests__/posts.test.ts
git commit -m "feat(astro): infer locale from filename and aggregate availableLocales"
```

---

## Task 3：Glob 收回 ja/en、`getActivePosts` 等加 locale 參數

**Files:**
- Modify: `src/content.config.ts`
- Modify: `src/lib/posts.ts`

- [ ] **Step 1: 修改 `src/content.config.ts`**

```ts
// src/content.config.ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const posts = defineCollection({
  loader: glob({
    // Phase 2: include translations (index.ja.md, index.en.md).
    pattern: ['**/*.md'],
    base: './src/content/posts',
  }),
  schema: z
    .object({
      slug: z.string().optional(),
      title: z.string().optional(),
      date: z.coerce.date().optional(),
      categories: z
        .union([z.array(z.string()), z.string()])
        .optional(),
      category: z.enum(['tech', 'life']).default('tech'),
      description: z.string().optional(),
      cover: z.string().optional(),
    })
    .passthrough(),
});

export const collections = { posts };
```

- [ ] **Step 2: 修改 `src/lib/posts.ts` 的 query API 加 locale 參數**

替換 `getActivePosts`、`getPostsByCategory`、`getArchivedPosts`、`getPostBySlug`：

```ts
export async function getActivePosts(locale: Locale): Promise<PostMeta[]> {
  return (await getAllPosts()).filter((p) => !p.archived && p.locale === locale);
}

export async function getPostsByCategory(
  category: 'tech' | 'life',
  locale: Locale,
): Promise<PostMeta[]> {
  return (await getActivePosts(locale)).filter((p) => p.category === category);
}

/**
 * Archives are zh-only by design (no translations exist for pre-2020 posts).
 */
export async function getArchivedPosts(): Promise<PostMeta[]> {
  return (await getAllPosts()).filter((p) => p.archived && p.locale === 'zh');
}

export async function getPostBySlug(
  locale: Locale,
  slug: string,
): Promise<PostMeta | null> {
  const all = await getAllPosts();
  return all.find((p) => p.locale === locale && p.slug === slug) ?? null;
}
```

- [ ] **Step 3: 試 build（預期會掛在 page caller 還沒更新 API）**

```bash
npx astro check 2>&1 | head -50
```

Expected: 多筆 type error，譬如 `getActivePosts() expected 1 argument, got 0` 出現在 page 檔。**這是預期**——下一個 task 修。

- [ ] **Step 4: Commit**

```bash
git add src/content.config.ts src/lib/posts.ts
git commit -m "feat(astro): include ja/en translations in glob and add locale param to query API"
```

---

## Task 4：更新 `[locale]/index.astro`、`tech.astro`、`life.astro` 為三 locale

**Files:**
- Modify: `src/pages/[locale]/index.astro`
- Modify: `src/pages/[locale]/tech.astro`
- Modify: `src/pages/[locale]/life.astro`

- [ ] **Step 1: 修改 `src/pages/[locale]/index.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getActivePosts } from '../../lib/posts';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const posts = await getActivePosts(locale);
const heading = t(locale).nav.home;
---
<BaseLayout title="Yuren's Blog" lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1 class="page-title">{heading}</h1>
    <PostList locale={locale} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title {
    font-size: 2.25rem;
    margin-bottom: 2rem;
  }
</style>
```

注意：`BaseLayout` 已支援 `lang` prop；`Header` 在 Task 9 才加 `pathname` 與 LanguageSwitcher，這裡先傳即可（多餘 prop 不會壞掉）。LanguageNotice 在 Task 11 補。

- [ ] **Step 2: 修改 `src/pages/[locale]/tech.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getPostsByCategory } from '../../lib/posts';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const posts = await getPostsByCategory('tech', locale);
const heading = t(locale).nav.tech;
---
<BaseLayout title={`${heading} · Yuren's Blog`} lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1 class="page-title">{heading}</h1>
    <PostList locale={locale} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **Step 3: 修改 `src/pages/[locale]/life.astro`**

跟 tech.astro 完全一致，只差 `'tech'` 改 `'life'`、`heading = t(locale).nav.life`、title 對應改：

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getPostsByCategory } from '../../lib/posts';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const posts = await getPostsByCategory('life', locale);
const heading = t(locale).nav.life;
---
<BaseLayout title={`${heading} · Yuren's Blog`} lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1 class="page-title">{heading}</h1>
    <PostList locale={locale} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **Step 4: 確認 Header 暫時容忍新 prop**

`Header.astro` 目前 props 只宣告 `locale`，新增的 `pathname` 雖會被忽略但 Astro 不會 error（Astro component 多餘 prop 不嚴格）。Task 9 才正式新增 props 宣告。

- [ ] **Step 5: 試 build（archives + posts/[slug] 還沒改，會掛）**

```bash
npx astro check 2>&1 | head -30
```

Expected: 仍有 `archives.astro` 與 `posts/[slug].astro` 相關 type error。**OK，下個 task 修。**

- [ ] **Step 6: Commit**

```bash
git add src/pages/\[locale\]/index.astro src/pages/\[locale\]/tech.astro src/pages/\[locale\]/life.astro
git commit -m "feat(astro): wire index/tech/life pages to all three locales"
```

---

## Task 5：更新 `archives.astro` 維持 zh-only

**Files:**
- Modify: `src/pages/[locale]/archives.astro`

- [ ] **Step 1: 修改 `src/pages/[locale]/archives.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getArchivedPosts } from '../../lib/posts';
import { type Locale, t } from '../../lib/i18n';

// Archives are zh-only by design (Phase 2 spec).
export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const locale = Astro.params.locale as Locale;
const posts = await getArchivedPosts();
const heading = t(locale).nav.archives;
---
<BaseLayout title={`${heading} · Yuren's Blog`} lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1 class="page-title">{heading}</h1>
    <PostList locale={locale} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/\[locale\]/archives.astro
git commit -m "feat(astro): keep archives zh-only in Phase 2"
```

---

## Task 6：更新 `about.astro` 與 `subscription.astro` 為三 locale

**Files:**
- Modify: `src/pages/[locale]/about.astro`
- Modify: `src/pages/[locale]/subscription.astro`

- [ ] **Step 1: 修改 `src/pages/[locale]/about.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const heading = t(locale).nav.about;
---
<BaseLayout title={`${heading} · Yuren's Blog`} lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1>{heading}</h1>
    <p>About 頁面內容稍後遷移。</p>
  </main>
  <Footer />
</BaseLayout>

<style>
  h1 { font-size: 2.25rem; margin-bottom: 1.5rem; }
</style>
```

- [ ] **Step 2: 修改 `src/pages/[locale]/subscription.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const heading = t(locale).nav.subscription;
---
<BaseLayout title={`${heading} · Yuren's Blog`} lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1>{heading}</h1>
    <p>訂閱頁面內容稍後遷移。</p>
  </main>
  <Footer />
</BaseLayout>

<style>
  h1 { font-size: 2.25rem; margin-bottom: 1.5rem; }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/\[locale\]/about.astro src/pages/\[locale\]/subscription.astro
git commit -m "feat(astro): wire about/subscription pages to all three locales"
```

---

## Task 7：更新 `posts/[slug].astro` 由實際 (locale, slug) 對驅動

**Files:**
- Modify: `src/pages/[locale]/posts/[slug].astro`

- [ ] **Step 1: 修改 `src/pages/[locale]/posts/[slug].astro`**

```astro
---
import PostLayout from '../../../layouts/PostLayout.astro';
import { getAllPosts } from '../../../lib/posts';
import { render } from 'astro:content';
import type { Locale } from '../../../lib/i18n';

export async function getStaticPaths() {
  const posts = await getAllPosts();
  return posts.map((post) => ({
    params: { locale: post.locale, slug: post.slug },
    props: { post },
  }));
}

const locale = Astro.params.locale as Locale;
const { post } = Astro.props;
const { Content } = await render(post.entry);
---
<PostLayout locale={locale} post={post}>
  <Content />
</PostLayout>
```

- [ ] **Step 2: 試 build**

```bash
npx astro build 2>&1 | tail -30
```

Expected: build 成功、頁數約 1564（1494 zh + 30 ja + 30 en + 約 6 個 ja/en 列表分類 + 4 個 about/subscription）。LanguageSwitcher、ArticleLanguageIndicator、LanguageNotice、hreflang 還沒做，但站台應該能 build。

如果 build 失敗，回頭檢查 Task 2/3 的修改。

- [ ] **Step 3: 抽樣驗證頁面存在**

```bash
ls dist/zh dist/ja dist/en | head -30
ls dist/ja/posts | wc -l   # expected: 30
ls dist/en/posts | wc -l   # expected: 30
ls dist/zh/archives 2>/dev/null && echo "zh archives ok"
ls dist/ja/archives 2>/dev/null && echo "BUG: ja archives should not exist"
ls dist/en/archives 2>/dev/null && echo "BUG: en archives should not exist"
```

Expected: ja/en archives 不存在；ja/en posts 各 30 篇。

- [ ] **Step 4: Commit**

```bash
git add src/pages/\[locale\]/posts/\[slug\].astro
git commit -m "feat(astro): generate post pages from actual (locale, slug) pairs"
```

---

## Task 8：`buildLanguageLinks` 純函式（給 LanguageSwitcher 用）

**Files:**
- Create: `src/lib/__tests__/language-switcher-links.test.ts`
- Modify: `src/lib/i18n.ts`（在最末加函式）

把 LanguageSwitcher 的連結邏輯抽成可測純函式，再讓 component 呼叫它。

- [ ] **Step 1: 寫失敗測試**

```ts
// src/lib/__tests__/language-switcher-links.test.ts
import { describe, it, expect } from 'vitest';
import { buildLanguageLinks } from '../i18n';

describe('buildLanguageLinks', () => {
  it('on home page: keeps the path, swaps locale prefix', () => {
    const links = buildLanguageLinks({
      currentLocale: 'zh',
      pathname: '/zh',
      isPostPage: false,
    });
    expect(links).toEqual([
      { locale: 'ja', href: '/ja' },
      { locale: 'en', href: '/en' },
    ]);
  });

  it('on category page: keeps the suffix, swaps locale prefix', () => {
    const links = buildLanguageLinks({
      currentLocale: 'zh',
      pathname: '/zh/tech',
      isPostPage: false,
    });
    expect(links).toEqual([
      { locale: 'ja', href: '/ja/tech' },
      { locale: 'en', href: '/en/tech' },
    ]);
  });

  it('on post page with full availableLocales: links to translation', () => {
    const links = buildLanguageLinks({
      currentLocale: 'zh',
      pathname: '/zh/posts/foo',
      isPostPage: true,
      slug: 'foo',
      availableLocales: ['zh', 'ja', 'en'],
    });
    expect(links).toEqual([
      { locale: 'ja', href: '/ja/posts/foo' },
      { locale: 'en', href: '/en/posts/foo' },
    ]);
  });

  it('on post page with partial availableLocales: links missing locale to home', () => {
    const links = buildLanguageLinks({
      currentLocale: 'zh',
      pathname: '/zh/posts/foo',
      isPostPage: true,
      slug: 'foo',
      availableLocales: ['zh', 'ja'],
    });
    expect(links).toEqual([
      { locale: 'ja', href: '/ja/posts/foo' },
      { locale: 'en', href: '/en' },
    ]);
  });

  it('excludes the current locale from results', () => {
    const links = buildLanguageLinks({
      currentLocale: 'ja',
      pathname: '/ja/tech',
      isPostPage: false,
    });
    expect(links.map((l) => l.locale)).toEqual(['zh', 'en']);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/language-switcher-links.test.ts
```

Expected: FAIL（function 未 export）

- [ ] **Step 3: 在 `src/lib/i18n.ts` 末尾加函式**

```ts
export interface LanguageLink {
  locale: Locale;
  href: string;
}

export interface BuildLanguageLinksInput {
  currentLocale: Locale;
  pathname: string;
  isPostPage: boolean;
  slug?: string;
  availableLocales?: Locale[];
}

/**
 * Build target hrefs for switching language from the current page.
 *
 * - Non-post pages: replace the locale prefix while preserving the rest of the path.
 * - Post pages: if the target locale has a translation, link to /{target}/posts/{slug};
 *   otherwise link to the target locale's home /{target}.
 */
export function buildLanguageLinks(input: BuildLanguageLinksInput): LanguageLink[] {
  const { currentLocale, pathname, isPostPage, slug, availableLocales } = input;
  const others = LOCALES.filter((l) => l !== currentLocale);
  return others.map((target) => {
    if (isPostPage && slug && availableLocales?.includes(target)) {
      return { locale: target, href: `/${target}/posts/${slug}` };
    }
    if (isPostPage) {
      return { locale: target, href: `/${target}` };
    }
    // Non-post page: swap prefix.
    const stripped = pathname.replace(/^\/(zh|ja|en)/, '');
    return { locale: target, href: `/${target}${stripped}` };
  });
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npx vitest run
```

Expected: PASS（全部）

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n.ts src/lib/__tests__/language-switcher-links.test.ts
git commit -m "feat(astro): add buildLanguageLinks pure function"
```

---

## Task 9：建立 `LanguageSwitcher.astro` 並接到 Header

**Files:**
- Create: `src/components/LanguageSwitcher.astro`
- Modify: `src/components/Header.astro`

LanguageSwitcher 用原生 `<details>`/`<summary>` 實作 dropdown，無需 JS、無 ID 衝突。

- [ ] **Step 1: 建立 `src/components/LanguageSwitcher.astro`**

```astro
---
import { buildLanguageLinks, LANGUAGE_NAMES, t, type Locale } from '../lib/i18n';

interface Props {
  currentLocale: Locale;
  pathname: string;
  isPostPage?: boolean;
  slug?: string;
  availableLocales?: Locale[];
}

const {
  currentLocale,
  pathname,
  isPostPage = false,
  slug,
  availableLocales,
} = Astro.props;

const links = buildLanguageLinks({
  currentLocale,
  pathname,
  isPostPage,
  slug,
  availableLocales,
});

const switchLabel = t(currentLocale).switchLanguage;
---
<details class="language-switcher">
  <summary aria-label={switchLabel}>
    <span aria-hidden="true">🌐</span>
    <span class="current">{LANGUAGE_NAMES[currentLocale]}</span>
  </summary>
  <ul>
    {links.map((link) => (
      <li>
        <a href={link.href}>{LANGUAGE_NAMES[link.locale]}</a>
      </li>
    ))}
  </ul>
</details>

<style>
  .language-switcher {
    position: relative;
    font-size: 0.9rem;
  }
  summary {
    list-style: none;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
  }
  summary::-webkit-details-marker { display: none; }
  summary:hover { background: #f3f3f3; }
  ul {
    position: absolute;
    right: 0;
    top: 100%;
    margin: 0.25rem 0 0 0;
    padding: 0.25rem 0;
    list-style: none;
    background: #fff;
    border: 1px solid #e5e5e5;
    border-radius: 4px;
    min-width: 8rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
    z-index: 10;
  }
  li a {
    display: block;
    padding: 0.4rem 0.75rem;
    color: inherit;
    text-decoration: none;
  }
  li a:hover { background: #f3f3f3; }
</style>
```

- [ ] **Step 2: 修改 `src/components/Header.astro`**

```astro
---
import LanguageSwitcher from './LanguageSwitcher.astro';
import { localePath, t, type Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  pathname: string;
  isPostPage?: boolean;
  slug?: string;
  availableLocales?: Locale[];
}

const {
  locale,
  pathname,
  isPostPage = false,
  slug,
  availableLocales,
} = Astro.props;

const text = t(locale);
const showArchives = locale === 'zh';
---
<header class="site-header">
  <div class="inner">
    <a href={localePath(locale)} class="brand">Yuren's Blog</a>
    <nav>
      <a href={localePath(locale, 'tech')}>{text.nav.tech}</a>
      <a href={localePath(locale, 'life')}>{text.nav.life}</a>
      {showArchives && <a href={localePath(locale, 'archives')}>{text.nav.archives}</a>}
      <a href={localePath(locale, 'about')}>{text.nav.about}</a>
    </nav>
    <LanguageSwitcher
      currentLocale={locale}
      pathname={pathname}
      isPostPage={isPostPage}
      slug={slug}
      availableLocales={availableLocales}
    />
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid #e5e5e5;
  }
  .inner {
    max-width: 48rem;
    margin: 0 auto;
    padding: 1rem;
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .brand {
    font-family: var(--font-serif), serif;
    font-size: 1.25rem;
    font-weight: 700;
  }
  nav {
    display: flex;
    gap: 1.25rem;
    font-size: 0.95rem;
  }
</style>
```

- [ ] **Step 3: 跑 build 確認沒打破任何頁面**

```bash
npx astro build 2>&1 | tail -20
```

Expected: build 成功。

- [ ] **Step 4: 抽樣 grep 產出的 HTML 確認 LanguageSwitcher 出現**

```bash
grep -l "language-switcher" dist/zh/index.html dist/ja/index.html dist/en/index.html
```

Expected: 三個檔案全部 match。

```bash
grep "Archives\|歸檔\|アーカイブ" dist/ja/index.html
```

Expected: 無 match（ja 不顯示 archives）。

- [ ] **Step 5: Commit**

```bash
git add src/components/LanguageSwitcher.astro src/components/Header.astro
git commit -m "feat(astro): add LanguageSwitcher and translate Header nav"
```

---

## Task 10：建立 `ArticleLanguageIndicator.astro` 並掛到 PostLayout

**Files:**
- Create: `src/components/ArticleLanguageIndicator.astro`
- Modify: `src/layouts/PostLayout.astro`

- [ ] **Step 1: 建立 `src/components/ArticleLanguageIndicator.astro`**

```astro
---
import { LANGUAGE_NAMES, t, type Locale } from '../lib/i18n';

interface Props {
  currentLocale: Locale;
  availableLocales: Locale[];
  slug: string;
}

const { currentLocale, availableLocales, slug } = Astro.props;
const others = availableLocales.filter((l) => l !== currentLocale);
const label = t(currentLocale).post.alsoAvailableIn;
---
{others.length > 0 && (
  <p class="article-language-indicator">
    <span>{label}</span>
    {others.map((l, i) => (
      <>
        {i > 0 && <span class="sep">、</span>}
        <a href={`/${l}/posts/${slug}`}>{LANGUAGE_NAMES[l]}</a>
      </>
    ))}
  </p>
)}

<style>
  .article-language-indicator {
    font-size: 0.85rem;
    color: #666;
    margin: 0 0 1rem 0;
  }
  .article-language-indicator a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .sep { margin: 0 0.25rem; }
</style>
```

- [ ] **Step 2: 修改 `src/layouts/PostLayout.astro`**

```astro
---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PostMeta from '../components/PostMeta.astro';
import ArticleLanguageIndicator from '../components/ArticleLanguageIndicator.astro';
import type { PostMeta as PostMetaType } from '../lib/posts';
import { HTML_LANG, HREFLANG, type Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  post: PostMetaType;
}

const { locale, post } = Astro.props;
const pageTitle = `${post.title} · Yuren's Blog`;
const siteUrl = 'https://yurenju.blog';
---
<BaseLayout title={pageTitle} description={post.description} lang={HTML_LANG[locale]}>
  <Fragment slot="head">
    {post.availableLocales.map((l) => (
      <link
        rel="alternate"
        hreflang={HREFLANG[l]}
        href={`${siteUrl}/${l}/posts/${post.slug}`}
      />
    ))}
    <link rel="alternate" hreflang="x-default" href={`${siteUrl}/zh/posts/${post.slug}`} />
  </Fragment>
  <Header
    locale={locale}
    pathname={Astro.url.pathname}
    isPostPage={true}
    slug={post.slug}
    availableLocales={post.availableLocales}
  />
  <main class="container">
    <article>
      <header class="post-head">
        <h1>{post.title}</h1>
        <PostMeta date={post.date} category={post.category} />
        <ArticleLanguageIndicator
          currentLocale={locale}
          availableLocales={post.availableLocales}
          slug={post.slug}
        />
      </header>
      <div class="prose">
        <slot />
      </div>
    </article>
  </main>
  <Footer />
</BaseLayout>

<style>
  .post-head {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid #eee;
  }
  .post-head h1 {
    font-size: 2.25rem;
    margin-bottom: 0.75rem;
  }
</style>
```

- [ ] **Step 3: 修改 `src/layouts/BaseLayout.astro` 加 head slot**

讀取後確認是否已有 `<slot name="head" />` 在 `<head>` 內，沒有就加：

```astro
---
import { Font } from 'astro:assets';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  lang?: string;
}

const { title, description, lang = 'zh-Hant-TW' } = Astro.props;
const siteDescription =
  description ??
  '撰寫就是一種思考方式，發佈與分享只是副產品，而真正的意義是在自我的知識脈絡裡面有了歸屬與連結。';
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={siteDescription} />
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <Font cssVariable="--font-sans" preload />
    <Font cssVariable="--font-serif" preload />
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>
```

- [ ] **Step 4: 試 build 與抽樣驗證 hreflang**

```bash
npx astro build 2>&1 | tail -10
```

Expected: build 成功。

選一篇有翻譯的 slug（例如 `2024-02-02_semaphore`，可由 `ls dist/ja/posts/ | head -3` 確認），grep hreflang：

```bash
grep -E 'hreflang|alsoAvailableIn|Also available' dist/zh/posts/2024-02-02_semaphore/index.html | head -10
```

Expected: 看到 zh/ja/en 各一條 alternate + x-default、與 ArticleLanguageIndicator 段落。

選一篇無翻譯的 zh-only 文章（譬如某 archives 內），grep hreflang：

```bash
grep -E 'hreflang' dist/zh/posts/<some-zh-only-slug>/index.html | head -10
```

Expected: 只有 zh + x-default 兩條，沒有 ja/en。

- [ ] **Step 5: Commit**

```bash
git add src/components/ArticleLanguageIndicator.astro src/layouts/PostLayout.astro src/layouts/BaseLayout.astro
git commit -m "feat(astro): add hreflang alternates and ArticleLanguageIndicator on post pages"
```

---

## Task 11：建立 `LanguageNotice.astro` 並掛到 ja/en 首頁

**Files:**
- Create: `src/components/LanguageNotice.astro`
- Modify: `src/pages/[locale]/index.astro`

- [ ] **Step 1: 建立 `src/components/LanguageNotice.astro`**

```astro
---
import { localePath, t, type Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  postCount: number;
}

const { locale, postCount } = Astro.props;
const text = t(locale).languageNotice;
---
<aside class="language-notice">
  <p>{text.mainlyInChinese}。{text.currentlyHas} {postCount} {text.articles}。</p>
  <p>
    <a href={localePath('zh')}>{text.switchToChinese}</a>
  </p>
</aside>

<style>
  .language-notice {
    border: 1px solid #e5e5e5;
    border-radius: 6px;
    padding: 1rem 1.25rem;
    margin: 0 0 2rem 0;
    background: #fafafa;
    font-size: 0.9rem;
    color: #555;
  }
  .language-notice p { margin: 0; }
  .language-notice p + p { margin-top: 0.4rem; }
  .language-notice a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
```

- [ ] **Step 2: 修改 `src/pages/[locale]/index.astro` 加 LanguageNotice**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import LanguageNotice from '../../components/LanguageNotice.astro';
import { getActivePosts } from '../../lib/posts';
import { LOCALES, type Locale, t } from '../../lib/i18n';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const posts = await getActivePosts(locale);
const heading = t(locale).nav.home;
---
<BaseLayout title="Yuren's Blog" lang={locale}>
  <Header locale={locale} pathname={Astro.url.pathname} />
  <main class="container">
    <h1 class="page-title">{heading}</h1>
    {locale !== 'zh' && <LanguageNotice locale={locale} postCount={posts.length} />}
    <PostList locale={locale} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title {
    font-size: 2.25rem;
    margin-bottom: 2rem;
  }
</style>
```

- [ ] **Step 3: Build 與抽樣驗證**

```bash
npx astro build 2>&1 | tail -5
```

Expected: build 成功。

```bash
grep -E 'language-notice|mainly|主要' dist/ja/index.html | head -5
grep -E 'language-notice' dist/zh/index.html
```

Expected: ja/en 首頁有 notice、zh 首頁沒有。

- [ ] **Step 4: Commit**

```bash
git add src/components/LanguageNotice.astro src/pages/\[locale\]/index.astro
git commit -m "feat(astro): show LanguageNotice on ja/en home pages"
```

---

## Task 12：更新 `PostList.astro` 用 `localePath()`

**Files:**
- Modify: `src/components/PostList.astro`

把 hardcoded URL `/${locale}/posts/${slug}` 改用 `localePath()`，避免遺漏點。

- [ ] **Step 1: 修改 `src/components/PostList.astro`**

只改 import 與 `<a href>`：

```astro
---
import { Image } from 'astro:assets';
import type { PostMeta } from '../lib/posts';
import { groupByYear } from '../lib/posts';
import { localePath, type Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  posts: PostMeta[];
}
const { locale, posts } = Astro.props;
const grouped = groupByYear(posts);
const years = [...grouped.keys()].sort((a, b) => b - a);
---
<div class="post-list">
  {years.map((year) => (
    <section class="year">
      <h2>{year}</h2>
      <ul>
        {grouped.get(year)!.map((post) => (
          <li>
            <a href={localePath(locale, 'posts', post.slug)}>
              <span class="date">{post.date.toISOString().slice(5, 10)}</span>
              <span class="title">{post.title}</span>
              {post.cover && (
                <span class="cover">
                  <Image
                    src={post.cover}
                    alt=""
                    widths={[120, 240]}
                    sizes="120px"
                    loading="lazy"
                  />
                </span>
              )}
            </a>
          </li>
        ))}
      </ul>
    </section>
  ))}
</div>

<style>
  .year { margin-bottom: 3rem; }
  .year h2 {
    font-family: var(--font-serif), serif;
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
    color: #444;
  }
  ul { list-style: none; padding: 0; }
  li { margin: 0.75rem 0; }
  li a {
    display: flex;
    gap: 1rem;
    align-items: center;
  }
  .date {
    font-family: var(--font-serif), serif;
    font-size: 0.875rem;
    color: #888;
    min-width: 4rem;
    letter-spacing: 0;
  }
  .title { flex: 1; }
  .cover { flex-shrink: 0; }
  .cover :global(img) {
    width: 120px;
    height: auto;
    border-radius: 4px;
    display: block;
  }
  li a:hover .title { text-decoration: underline; text-underline-offset: 4px; }
</style>
```

- [ ] **Step 2: Build 確認沒壞**

```bash
npx astro build 2>&1 | tail -5
```

Expected: build 成功。

- [ ] **Step 3: Commit**

```bash
git add src/components/PostList.astro
git commit -m "refactor(astro): use localePath helper in PostList"
```

---

## Task 13：完整驗收（手動）

**Files:** 無修改

- [ ] **Step 1: Clean build**

```bash
rm -rf dist
npx astro build 2>&1 | tail -20
```

Expected: build 成功，build 時間 < 60s。記錄頁數。

- [ ] **Step 2: 跑全部單元測試**

```bash
npx vitest run
```

Expected: PASS（全部）

- [ ] **Step 3: 各驗收項目逐條打勾**

對照 spec 「驗收」章節：

```bash
# 1. Build 綠燈、頁數
ls dist/zh/posts | wc -l   # ≈ 1494
ls dist/ja/posts | wc -l   # 30
ls dist/en/posts | wc -l   # 30

# 2. 列表頁過濾
ls dist/ja dist/en | sort -u | head
# 應有 index.html, tech, life, about, subscription，無 archives

# 3. 文章頁 404 行為（無翻譯 slug 在 ja/en 不存在）
ls dist/ja/posts/2007-09-19_facebook 2>/dev/null && echo "BUG" || echo "OK: 無翻譯不產生"

# 4. Archives 限定 zh
ls dist/zh/archives 2>/dev/null && echo "OK"
ls dist/ja/archives 2>/dev/null && echo "BUG" || echo "OK"

# 5. 跨語言 metadata
grep 'hreflang' dist/zh/posts/2024-02-02_semaphore/index.html | head

# 6. Notice
grep 'language-notice' dist/ja/index.html | head -1
grep 'language-notice' dist/zh/index.html  # 應為 empty

# 7. LanguageSwitcher 連結（在 ja 文章頁有翻譯時連到 zh 對應）
grep -A1 'language-switcher' dist/ja/posts/2024-02-02_semaphore/index.html | head

# 8. Header Archives 在 ja/en 隱藏
grep -i 'archives\|歸檔\|アーカイブ' dist/ja/index.html | head

# 9. Build 時間（看 step 1 紀錄）
```

- [ ] **Step 4: 用 preview 抽樣肉眼驗收**

啟動 preview，挑幾條路徑肉眼確認：

`/zh/`、`/ja/`、`/en/`、`/ja/tech`、`/en/posts/<某 en slug>`、`/zh/posts/<同 dirname slug>`（應顯示中文且 ArticleLanguageIndicator 連到 ja/en 翻譯）、`/ja/archives`（應 404）、語言切換器在文章頁切到「無翻譯 locale」應跳目標首頁。

（用 `mcp__Claude_Preview__preview_start` 啟 dev server。）

- [ ] **Step 5: Commit final（如果驗收過程中發現小問題並修了）**

如有 fixup commit 整理進去。

---

## 自我檢查（plan author）

- ✅ Spec 涵蓋：每個 spec 「元件邊界」與「驗收」項目都有對應 task。
- ✅ 無 placeholder。
- ✅ 型別/簽章一致：`getActivePosts(locale)`、`getPostsByCategory(category, locale)`、`getArchivedPosts()`（無參數）、`getPostBySlug(locale, slug)` 在 Task 3 與被呼叫處（Task 4、5、6、7）一致。
- ✅ `PostMeta.locale` 與 `PostMeta.availableLocales` 在 Task 2 定義、Task 7 / 10 / 11 使用。
- ✅ `buildLanguageLinks` 簽章 (Task 8) 與 LanguageSwitcher 呼叫 (Task 9) 一致。

# Phase 3 — RSS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 `@astrojs/rss` 重做 12 隻 RSS feed，修掉 Next.js prod 的 `<language>` 硬寫與三語混雜 bug；每隻 feed 只裝對應 locale 文章、最新 20 篇全文、不含 archives。

**Architecture:** Endpoint route 在 `src/pages/rss/...xml.ts`，全部呼叫共用的 `src/lib/rss-feed.ts` (`buildFeedItems` 純函式 + `channelMeta` helper)。Item 全文 HTML 由 Astro Container API 渲染（與頁面內容一致）。Legacy 無 prefix 的 4 隻 (`/rss.xml`、`/rss/{tech,life}.xml`) 內容對齊 zh feed，唯一差別是 `<atom:link rel="self">` 指向自身 URL。

**Tech Stack:** Astro 6.x、`@astrojs/rss`、Astro Container API（experimental，4.9+ 起穩定可用）、TypeScript、vitest。

**Spec：** `docs/superpowers/specs/2026-05-02-phase-3-rss-design.md`

**所有路徑相對於 `astro/` 子目錄。** 工作目錄請固定在 `astro/`。**請先建分支：`git checkout -b phase-3-rss`。**

---

## 檔案結構

**Create:**
- `src/lib/rss-feed.ts` — 純資料層：`channelMeta()` 與 `buildFeedItems()`
- `src/lib/__tests__/rss-feed.test.ts` — 兩個 helper 的單元測試
- `src/pages/rss/[locale].xml.ts` — per-locale all-posts feed
- `src/pages/rss/[locale]/[category].xml.ts` — per-locale per-category feed
- `src/pages/rss.xml.ts` — legacy alias，內容 = zh all
- `src/pages/rss/[category].xml.ts` — legacy alias，內容 = zh per-category

**Modify:**
- `astro/package.json` — add `@astrojs/rss` dep
- `src/lib/i18n.ts` — extend `UiText` 加 `site.description` 與 `rss.{allPosts,tech,life}`
- `src/lib/__tests__/i18n.test.ts` — 加幾個新 key 的測試（避免破壞既有）
- `docs/research/2026-04-29-astro-migration-roadmap.md` — Phase 3 標記完成（最後一個 task）

---

## Task 1：建分支並安裝 `@astrojs/rss`

**Files:**
- Modify: `package.json`、`package-lock.json`

- [ ] **Step 1: 建分支**

```bash
git checkout -b phase-3-rss
```

- [ ] **Step 2: 安裝 dep**

```bash
npm install @astrojs/rss
```

- [ ] **Step 3: 確認 import 可解析**

建立暫時檔 `/tmp/check-rss.mjs`：

```js
import rss from '@astrojs/rss';
console.log(typeof rss);
```

```bash
node --input-type=module -e "import rss from '@astrojs/rss'; console.log(typeof rss);"
```

Expected: 印 `function`。

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore(astro): add @astrojs/rss dependency for Phase 3"
```

---

## Task 2：Spike — 驗證 Container API 能正確渲染含圖文章

**Files:**
- Create: `scripts/rss-spike.ts`（暫時，後刪）

Spec 提到「markdown-it 路線」對含圖文章可能爆破圖（因為 Astro image pipeline 處理過的 hashed URL 拿不到）。這個 task 直接驗 Container API 路線可行；目標：**確認 Container API 能渲染一篇含 markdown image 的 entry，輸出 HTML 含 `<img src="/_astro/...hash..." />` 形式的 hashed URL**。

- [ ] **Step 1: 建立 spike script**

`scripts/rss-spike.ts`：

```ts
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render, getCollection } from 'astro:content';

const all = await getCollection('posts');
// Pick a post known to have markdown images (semaphore is a good candidate)
const target = all.find((e) => e.id.includes('2024-02-02_semaphore'));
if (!target) {
  console.error('Target entry not found');
  process.exit(1);
}

const container = await AstroContainer.create();
const { Content } = await render(target);
const html = await container.renderToString(Content);

console.log('--- HTML length:', html.length, '---');
console.log('--- First img tag ---');
const m = html.match(/<img[^>]+>/);
console.log(m ? m[0] : '(no img found)');
console.log('--- /img check ---');
```

- [ ] **Step 2: 跑 spike**

Astro container 需要 Astro runtime，不能用 plain node。改用：

```bash
npx astro dev &
sleep 5
# 不對 — container API 在 build 時呼叫即可。改用 endpoint 跑。
```

實際上 spike 最簡單方式是把 spike 程式碼直接寫成一個 endpoint 跑 build 看輸出。改寫成下面方式：

把 spike 內容改貼到 `src/pages/rss-spike.json.ts`（暫時 endpoint）：

```ts
import type { APIRoute } from 'astro';
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render, getCollection } from 'astro:content';

export const GET: APIRoute = async () => {
  const all = await getCollection('posts');
  const target = all.find((e) => e.id.includes('2024-02-02_semaphore'));
  if (!target) {
    return new Response(JSON.stringify({ error: 'not found' }), { status: 404 });
  }
  const container = await AstroContainer.create();
  const { Content } = await render(target);
  const html = await container.renderToString(Content);
  const imgMatch = html.match(/<img[^>]+>/);
  return new Response(JSON.stringify({
    htmlLength: html.length,
    firstImg: imgMatch ? imgMatch[0] : null,
  }, null, 2), { headers: { 'Content-Type': 'application/json' } });
};
```

刪 `scripts/rss-spike.ts`。

- [ ] **Step 3: Build 並讀 spike 輸出**

```bash
npx astro build 2>&1 | tail -5
cat dist/rss-spike.json
```

Expected JSON output：
```json
{
  "htmlLength": <some-number-larger-than-5000>,
  "firstImg": "<img src=\"/_astro/...something.webp\" ... />"
}
```

關鍵驗證：`firstImg` 含 `/_astro/` 前綴的 hashed URL（不是 `images/0.png`）。

如果 firstImg 是 `null` 或顯示原始路徑（如 `images/0.png`、`/posts/...`）：**STOP — escalate to controller**。spec 的 Container API 假設不成立，需要重新設計 item content 路線。

如果 firstImg 是 hashed URL：**Container API 路線可行，繼續**。

- [ ] **Step 4: 移除 spike endpoint**

```bash
rm src/pages/rss-spike.json.ts
```

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore(astro): verify Container API renders markdown images correctly"
```

---

## Task 3：擴充 `src/lib/i18n.ts` 的 UiText

**Files:**
- Modify: `src/lib/i18n.ts`
- Modify: `src/lib/__tests__/i18n.test.ts`

- [ ] **Step 1: 寫失敗測試（加在現有 `i18n.test.ts` 末尾）**

```ts
describe('UI_TEXT site.description', () => {
  it('zh has zh description', () => {
    expect(t('zh').site.description).toContain('撰寫');
  });
  it('ja has ja description', () => {
    expect(t('ja').site.description).toContain('書く');
  });
  it('en has en description', () => {
    expect(t('en').site.description).toContain('Writing');
  });
});

describe('UI_TEXT rss labels', () => {
  it('per-locale rss labels for tech/life/allPosts', () => {
    expect(t('zh').rss.tech).toBe('技術');
    expect(t('zh').rss.life).toBe('生活');
    expect(t('zh').rss.allPosts).toBe('全部文章');
    expect(t('ja').rss.tech).toBe('技術');
    expect(t('ja').rss.life).toBe('生活');
    expect(t('ja').rss.allPosts).toBe('すべての記事');
    expect(t('en').rss.tech).toBe('Tech');
    expect(t('en').rss.life).toBe('Life');
    expect(t('en').rss.allPosts).toBe('All Posts');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/i18n.test.ts
```

Expected: FAIL — `Cannot read property 'description' of undefined` 或 `rss undefined`。

- [ ] **Step 3: 修改 `src/lib/i18n.ts`**

Locate the `UiText` interface and add two new fields. Final shape:

```ts
export interface UiText {
  nav: {
    home: string;
    tech: string;
    life: string;
    archives: string;
    about: string;
    subscription: string;
  };
  post: {
    alsoAvailableIn: string;
  };
  languageNotice: {
    mainlyInChinese: string;
    currentlyHas: string;
    articles: string;
    switchToChinese: string;
  };
  switchLanguage: string;
  site: { description: string };
  rss: {
    allPosts: string;
    tech: string;
    life: string;
  };
}
```

Then in each locale's `UI_TEXT` entry add the two new top-level keys. Final structure for zh entry:

```ts
zh: {
  nav: { home: '首頁', tech: '技術', life: '生活', archives: '歸檔', about: '關於', subscription: '訂閱' },
  post: { alsoAvailableIn: '其他語言版本：' },
  languageNotice: {
    mainlyInChinese: '本站主要以繁體中文撰寫',
    currentlyHas: '目前有',
    articles: '篇文章',
    switchToChinese: '切換到中文版以瀏覽所有內容',
  },
  switchLanguage: '切換語言',
  site: {
    description: '撰寫就是一種思考方式，發佈與分享只是副產品，而真正的意義是在自我的知識脈絡裡面有了歸屬與連結。',
  },
  rss: {
    allPosts: '全部文章',
    tech: '技術',
    life: '生活',
  },
},
```

ja entry:

```ts
ja: {
  nav: { home: 'ホーム', tech: '技術', life: '生活', archives: 'アーカイブ', about: '概要', subscription: '購読' },
  post: { alsoAvailableIn: 'Also available in:' },
  languageNotice: {
    mainlyInChinese: 'このサイトは主に繁体字中国語で書かれています',
    currentlyHas: '現在',
    articles: '件の記事があります',
    switchToChinese: '中国語版に切り替えるとすべてのコンテンツをご覧いただけます',
  },
  switchLanguage: '言語切替',
  site: {
    description: '書くことは思考の一形態であり、公開と共有は副産物に過ぎません。真の意味は、自己の知識体系における帰属とつながりにあります。',
  },
  rss: {
    allPosts: 'すべての記事',
    tech: '技術',
    life: '生活',
  },
},
```

en entry:

```ts
en: {
  nav: { home: 'Home', tech: 'Tech', life: 'Life', archives: 'Archives', about: 'About', subscription: 'Subscribe' },
  post: { alsoAvailableIn: 'Also available in:' },
  languageNotice: {
    mainlyInChinese: 'This site is mainly written in Traditional Chinese',
    currentlyHas: 'Currently there are',
    articles: 'articles',
    switchToChinese: 'Switch to the Chinese version to browse all content',
  },
  switchLanguage: 'Switch language',
  site: {
    description: 'Writing is a form of thinking. Publishing and sharing are just byproducts. The real meaning lies in finding belonging and connections within one\'s own knowledge framework.',
  },
  rss: {
    allPosts: 'All Posts',
    tech: 'Tech',
    life: 'Life',
  },
},
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npx vitest run
```

Expected: PASS — total 51 (was 45, +6 new tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n.ts src/lib/__tests__/i18n.test.ts
git commit -m "feat(astro): add site.description and rss.* UI text"
```

---

## Task 4：實作 `channelMeta()` + 測試

**Files:**
- Create: `src/lib/rss-feed.ts`
- Create: `src/lib/__tests__/rss-feed.test.ts`

- [ ] **Step 1: 寫失敗測試**

```ts
// src/lib/__tests__/rss-feed.test.ts
import { describe, it, expect } from 'vitest';
import { channelMeta } from '../rss-feed';

describe('channelMeta', () => {
  it('zh all-posts has zh-Hant language and 全部文章 title suffix', () => {
    const m = channelMeta('zh', 'all');
    expect(m.title).toBe("Yuren's Blog - 全部文章");
    expect(m.language).toBe('zh-Hant');
    expect(m.description).toContain('撰寫');
  });

  it('ja tech has ja language and 技術 title suffix', () => {
    const m = channelMeta('ja', 'tech');
    expect(m.title).toBe("Yuren's Blog - 技術");
    expect(m.language).toBe('ja');
    expect(m.description).toContain('書く');
  });

  it('en life has en language and Life title suffix', () => {
    const m = channelMeta('en', 'life');
    expect(m.title).toBe("Yuren's Blog - Life");
    expect(m.language).toBe('en');
    expect(m.description).toContain('Writing');
  });

  it('zh tech', () => {
    const m = channelMeta('zh', 'tech');
    expect(m.title).toBe("Yuren's Blog - 技術");
    expect(m.language).toBe('zh-Hant');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/rss-feed.test.ts
```

Expected: FAIL — module not found.

- [ ] **Step 3: 實作 `src/lib/rss-feed.ts`（先只 channelMeta，buildFeedItems 下一個 task 加）**

```ts
import { t, type Locale } from './i18n';

export type FeedKind = 'all' | 'tech' | 'life';

export interface ChannelMeta {
  title: string;
  description: string;
  language: 'zh-Hant' | 'ja' | 'en';
}

const LANGUAGE_TAG: Record<Locale, ChannelMeta['language']> = {
  zh: 'zh-Hant',
  ja: 'ja',
  en: 'en',
};

/**
 * Build the channel-level metadata (title / description / language) for a feed.
 *
 * - title: "Yuren's Blog - {category-or-all label}"
 * - description: site description per locale
 * - language: zh-Hant / ja / en (NOT zh-tw)
 */
export function channelMeta(locale: Locale, kind: FeedKind): ChannelMeta {
  const text = t(locale);
  const suffix = kind === 'all' ? text.rss.allPosts : text.rss[kind];
  return {
    title: `Yuren's Blog - ${suffix}`,
    description: text.site.description,
    language: LANGUAGE_TAG[locale],
  };
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npx vitest run
```

Expected: PASS — total 55 (51 + 4 new).

- [ ] **Step 5: Commit**

```bash
git add src/lib/rss-feed.ts src/lib/__tests__/rss-feed.test.ts
git commit -m "feat(astro): add channelMeta helper for RSS feeds"
```

---

## Task 5：實作 `buildFeedItems()` + 測試

**Files:**
- Modify: `src/lib/rss-feed.ts`
- Modify: `src/lib/__tests__/rss-feed.test.ts`

`buildFeedItems()` 是純資料映射 — 給 `PostMeta[]`，回傳 `RSSFeedItem[]`（限 20，含全文 HTML）。
全文 HTML 渲染由 caller（endpoint）處理並注入，本函式只接 string。設計簽章為：

```ts
buildFeedItems(
  posts: PostMeta[],
  renderHtml: (post: PostMeta) => Promise<string>,
  locale: Locale,
): Promise<RSSFeedItem[]>
```

這樣本 helper 純粹、可測（mock `renderHtml`），endpoint 才注入 Container API 細節。

- [ ] **Step 1: 寫失敗測試（加在 rss-feed.test.ts 末尾）**

```ts
import { buildFeedItems } from '../rss-feed';
import type { PostMeta } from '../posts';

function makePost(overrides: Partial<PostMeta> = {}): PostMeta {
  // Minimal stub matching the fields buildFeedItems uses.
  return {
    entry: { id: 'stub' } as PostMeta['entry'],
    slug: 'stub',
    group: '2024',
    archived: false,
    category: 'tech',
    title: 'stub title',
    date: new Date('2024-01-01'),
    locale: 'zh',
    availableLocales: ['zh'],
    cover: null,
    ...overrides,
  };
}

describe('buildFeedItems', () => {
  const fakeRender = async (p: PostMeta) => `<p>html for ${p.slug}</p>`;

  it('caps at 20 items even if more posts given', async () => {
    const posts = Array.from({ length: 25 }, (_, i) =>
      makePost({ slug: `s${i}`, date: new Date(`2024-01-${String(i + 1).padStart(2, '0')}`) }),
    );
    const items = await buildFeedItems(posts, fakeRender, 'zh');
    expect(items).toHaveLength(20);
  });

  it('returns fewer items when posts < 20', async () => {
    const posts = [makePost({ slug: 'a' }), makePost({ slug: 'b' })];
    const items = await buildFeedItems(posts, fakeRender, 'zh');
    expect(items).toHaveLength(2);
  });

  it('preserves date-descending order from input', async () => {
    const posts = [
      makePost({ slug: 'old', date: new Date('2020-01-01') }),
      makePost({ slug: 'new', date: new Date('2024-01-01') }),
    ];
    // buildFeedItems trusts caller-supplied order; caller will pass already-sorted list
    const items = await buildFeedItems(posts, fakeRender, 'zh');
    expect(items[0]!.title).toBe('stub title');
    expect((items[0] as { link: string }).link).toContain('old');
  });

  it('item link includes locale prefix even for zh', async () => {
    const posts = [makePost({ slug: 'foo', locale: 'zh' })];
    const items = await buildFeedItems(posts, fakeRender, 'zh');
    expect((items[0] as { link: string }).link).toBe('/zh/posts/foo');
  });

  it('item link uses ja prefix for ja post', async () => {
    const posts = [makePost({ slug: 'foo', locale: 'ja' })];
    const items = await buildFeedItems(posts, fakeRender, 'ja');
    expect((items[0] as { link: string }).link).toBe('/ja/posts/foo');
  });

  it('item content is the rendered HTML', async () => {
    const posts = [makePost({ slug: 'foo' })];
    const items = await buildFeedItems(posts, fakeRender, 'zh');
    expect((items[0] as { content: string }).content).toBe('<p>html for foo</p>');
  });

  it('item categories localized via locale arg', async () => {
    const posts = [makePost({ slug: 'foo', category: 'tech' })];
    const items = await buildFeedItems(posts, fakeRender, 'ja');
    expect((items[0] as { categories: string[] }).categories).toEqual(['技術']);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
npx vitest run src/lib/__tests__/rss-feed.test.ts
```

Expected: FAIL — `buildFeedItems` is not exported.

- [ ] **Step 3: 擴充 `src/lib/rss-feed.ts`**

Append below `channelMeta`:

```ts
import type { RSSFeedItem } from '@astrojs/rss';
import type { PostMeta } from './posts';

const ITEM_LIMIT = 20;

/**
 * Build feed items for a feed.
 *
 * @param posts caller is responsible for filtering and sorting (date-desc).
 * @param renderHtml endpoint-supplied function that returns the full-content HTML for a post.
 * @param locale used to localize the per-item category label.
 */
export async function buildFeedItems(
  posts: PostMeta[],
  renderHtml: (post: PostMeta) => Promise<string>,
  locale: Locale,
): Promise<RSSFeedItem[]> {
  const sliced = posts.slice(0, ITEM_LIMIT);
  const text = t(locale);
  const items = await Promise.all(
    sliced.map(async (p) => {
      const html = await renderHtml(p);
      return {
        title: p.title,
        pubDate: p.date,
        link: `/${p.locale}/posts/${p.slug}`,
        content: html,
        categories: [text.rss[p.category]],
      } satisfies RSSFeedItem;
    }),
  );
  return items;
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npx vitest run
```

Expected: PASS — total 62 (55 + 7 new).

- [ ] **Step 5: Commit**

```bash
git add src/lib/rss-feed.ts src/lib/__tests__/rss-feed.test.ts
git commit -m "feat(astro): add buildFeedItems helper for RSS feeds"
```

---

## Task 6：endpoint `src/pages/rss/[locale].xml.ts`（per-locale all-posts）

**Files:**
- Create: `src/pages/rss/[locale].xml.ts`

- [ ] **Step 1: 建立 endpoint**

```ts
// src/pages/rss/[locale].xml.ts
import type { APIRoute, GetStaticPaths } from 'astro';
import rss from '@astrojs/rss';
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render } from 'astro:content';
import { LOCALES, type Locale } from '../../lib/i18n';
import { getActivePosts } from '../../lib/posts';
import { buildFeedItems, channelMeta } from '../../lib/rss-feed';

export const getStaticPaths: GetStaticPaths = () =>
  LOCALES.map((locale) => ({ params: { locale } }));

export const GET: APIRoute = async ({ params, site }) => {
  const locale = params.locale as Locale;
  const meta = channelMeta(locale, 'all');
  const posts = await getActivePosts(locale);

  const container = await AstroContainer.create();
  const renderHtml = async (post: typeof posts[number]): Promise<string> => {
    const { Content } = await render(post.entry);
    return await container.renderToString(Content);
  };

  const items = await buildFeedItems(posts, renderHtml, locale);

  return rss({
    title: meta.title,
    description: meta.description,
    site: site!,
    customData: `<language>${meta.language}</language>`,
    items,
  });
};
```

- [ ] **Step 2: Build & 抽樣驗證**

```bash
npx astro build 2>&1 | tail -10
```

Expected: build 成功。

```bash
ls -lh dist/rss/*.xml
```

Expected: `dist/rss/zh.xml`、`dist/rss/ja.xml`、`dist/rss/en.xml` 三檔。zh 約 200KB–數 MB（20 篇全文），ja/en 較小（≤30 篇全文 ≤ 1MB）。

```bash
xmllint --noout dist/rss/zh.xml dist/rss/ja.xml dist/rss/en.xml && echo OK
```

Expected: `OK`。

```bash
grep '<language>' dist/rss/zh.xml dist/rss/ja.xml dist/rss/en.xml
```

Expected:
```
dist/rss/zh.xml:        <language>zh-Hant</language>
dist/rss/ja.xml:        <language>ja</language>
dist/rss/en.xml:        <language>en</language>
```

```bash
# Item count per feed
for f in dist/rss/zh.xml dist/rss/ja.xml dist/rss/en.xml; do echo -n "$f: "; grep -c '<item>' "$f"; done
```

Expected: zh ≤ 20、ja ≤ 30、en ≤ 30；實際以 active posts 數為準。

```bash
# Verify ja feed has ja titles only (no zh content)
grep -m3 '<title>' dist/rss/ja.xml | tail -2
```

Expected: 應為日文 title（如 `<title><![CDATA[...日文...]]>` 或包含日文文字）。

- [ ] **Step 3: 跑全部測試（沒有新測試但確認沒爆既有）**

```bash
npx vitest run
```

Expected: 62 PASS。

- [ ] **Step 4: Commit**

```bash
git add src/pages/rss/\[locale\].xml.ts
git commit -m "feat(astro): add per-locale all-posts RSS feeds"
```

---

## Task 7：endpoint `src/pages/rss/[locale]/[category].xml.ts`（per-locale per-category）

**Files:**
- Create: `src/pages/rss/[locale]/[category].xml.ts`

- [ ] **Step 1: 建立 endpoint**

```ts
// src/pages/rss/[locale]/[category].xml.ts
import type { APIRoute, GetStaticPaths } from 'astro';
import rss from '@astrojs/rss';
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render } from 'astro:content';
import { LOCALES, type Locale } from '../../../lib/i18n';
import { getPostsByCategory } from '../../../lib/posts';
import { buildFeedItems, channelMeta, type FeedKind } from '../../../lib/rss-feed';

const CATEGORIES = ['tech', 'life'] as const;
type Category = (typeof CATEGORIES)[number];

export const getStaticPaths: GetStaticPaths = () =>
  LOCALES.flatMap((locale) =>
    CATEGORIES.map((category) => ({ params: { locale, category } })),
  );

export const GET: APIRoute = async ({ params, site }) => {
  const locale = params.locale as Locale;
  const category = params.category as Category;
  const meta = channelMeta(locale, category as FeedKind);
  const posts = await getPostsByCategory(category, locale);

  const container = await AstroContainer.create();
  const renderHtml = async (post: typeof posts[number]): Promise<string> => {
    const { Content } = await render(post.entry);
    return await container.renderToString(Content);
  };

  const items = await buildFeedItems(posts, renderHtml, locale);

  return rss({
    title: meta.title,
    description: meta.description,
    site: site!,
    customData: `<language>${meta.language}</language>`,
    items,
  });
};
```

- [ ] **Step 2: Build & 抽樣驗證**

```bash
npx astro build 2>&1 | tail -5
ls -lh dist/rss/zh dist/rss/ja dist/rss/en
```

Expected: 三個 dir 內各有 `tech.xml`、`life.xml`，共 6 個檔。

```bash
xmllint --noout dist/rss/zh/tech.xml dist/rss/zh/life.xml dist/rss/ja/tech.xml dist/rss/ja/life.xml dist/rss/en/tech.xml dist/rss/en/life.xml && echo OK
```

```bash
grep '<language>' dist/rss/ja/tech.xml dist/rss/en/life.xml
```

Expected: `ja` and `en`.

```bash
for f in dist/rss/zh/tech.xml dist/rss/zh/life.xml dist/rss/ja/tech.xml dist/rss/ja/life.xml dist/rss/en/tech.xml dist/rss/en/life.xml; do echo -n "$f: "; grep -c '<item>' "$f"; done
```

Expected counts based on Phase 2 verification: zh tech 20, zh life 20, ja tech 20, ja life 0, en tech 20, en life 0.

- [ ] **Step 3: Commit**

```bash
git add src/pages/rss/\[locale\]/\[category\].xml.ts
git commit -m "feat(astro): add per-locale per-category RSS feeds"
```

---

## Task 8：legacy `/rss.xml` (内容 = zh all)

**Files:**
- Create: `src/pages/rss.xml.ts`

- [ ] **Step 1: 建立 endpoint**

```ts
// src/pages/rss.xml.ts
import type { APIRoute } from 'astro';
import rss from '@astrojs/rss';
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render } from 'astro:content';
import { getActivePosts } from '../lib/posts';
import { buildFeedItems, channelMeta } from '../lib/rss-feed';

// Legacy alias: /rss.xml — content matches /rss/zh.xml.
export const GET: APIRoute = async ({ site }) => {
  const meta = channelMeta('zh', 'all');
  const posts = await getActivePosts('zh');

  const container = await AstroContainer.create();
  const renderHtml = async (post: typeof posts[number]): Promise<string> => {
    const { Content } = await render(post.entry);
    return await container.renderToString(Content);
  };

  const items = await buildFeedItems(posts, renderHtml, 'zh');

  return rss({
    title: meta.title,
    description: meta.description,
    site: site!,
    customData: `<language>${meta.language}</language>`,
    items,
  });
};
```

- [ ] **Step 2: Build & 驗證**

```bash
npx astro build 2>&1 | tail -5
xmllint --noout dist/rss.xml && echo OK
```

```bash
# Diff items between /rss.xml and /rss/zh.xml — should be identical except <atom:link self>
diff <(grep -v 'atom:link' dist/rss.xml) <(grep -v 'atom:link' dist/rss/zh.xml) | head -5
```

Expected: 兩檔扣掉 `atom:link` 那行後完全一致（zero diff lines）。

```bash
grep 'atom:link' dist/rss.xml dist/rss/zh.xml
```

Expected: 兩個 self-link 不同（前者 `/rss.xml`、後者 `/rss/zh.xml`）。

- [ ] **Step 3: Commit**

```bash
git add src/pages/rss.xml.ts
git commit -m "feat(astro): add legacy /rss.xml alias for zh all-posts"
```

---

## Task 9：legacy `/rss/[category].xml.ts`（zh per-category）

**Files:**
- Create: `src/pages/rss/[category].xml.ts`

- [ ] **Step 1: 建立 endpoint**

```ts
// src/pages/rss/[category].xml.ts
import type { APIRoute, GetStaticPaths } from 'astro';
import rss from '@astrojs/rss';
import { experimental_AstroContainer as AstroContainer } from 'astro/container';
import { render } from 'astro:content';
import { getPostsByCategory } from '../../lib/posts';
import { buildFeedItems, channelMeta, type FeedKind } from '../../lib/rss-feed';

const CATEGORIES = ['tech', 'life'] as const;
type Category = (typeof CATEGORIES)[number];

// Legacy alias: /rss/{tech,life}.xml — content matches /rss/zh/{tech,life}.xml.
export const getStaticPaths: GetStaticPaths = () =>
  CATEGORIES.map((category) => ({ params: { category } }));

export const GET: APIRoute = async ({ params, site }) => {
  const category = params.category as Category;
  const meta = channelMeta('zh', category as FeedKind);
  const posts = await getPostsByCategory(category, 'zh');

  const container = await AstroContainer.create();
  const renderHtml = async (post: typeof posts[number]): Promise<string> => {
    const { Content } = await render(post.entry);
    return await container.renderToString(Content);
  };

  const items = await buildFeedItems(posts, renderHtml, 'zh');

  return rss({
    title: meta.title,
    description: meta.description,
    site: site!,
    customData: `<language>${meta.language}</language>`,
    items,
  });
};
```

- [ ] **Step 2: Build & 驗證**

```bash
npx astro build 2>&1 | tail -5
xmllint --noout dist/rss/tech.xml dist/rss/life.xml && echo OK
```

```bash
diff <(grep -v 'atom:link' dist/rss/tech.xml) <(grep -v 'atom:link' dist/rss/zh/tech.xml) | head -5
diff <(grep -v 'atom:link' dist/rss/life.xml) <(grep -v 'atom:link' dist/rss/zh/life.xml) | head -5
```

Expected: 兩對 diff 都為空。

- [ ] **Step 3: Commit**

```bash
git add src/pages/rss/\[category\].xml.ts
git commit -m "feat(astro): add legacy /rss/{tech,life}.xml aliases for zh"
```

---

## Task 10：完整驗收 + roadmap 更新

**Files:**
- Modify: `docs/research/2026-04-29-astro-migration-roadmap.md`

- [ ] **Step 1: Clean build**

```bash
rm -rf dist
time npx astro build 2>&1 | tail -10
```

Expected: build 成功，時間 ≤ Phase 2 + 5s（Phase 2 baseline 32s，目標 < 37s）。

- [ ] **Step 2: 12 個 RSS 檔存在**

```bash
ls dist/rss.xml dist/rss/{zh,ja,en}.xml dist/rss/{tech,life}.xml dist/rss/{zh,ja,en}/{tech,life}.xml
```

Expected: 12 個檔全部存在，無 `No such file` 訊息。

- [ ] **Step 3: 全部 well-formed**

```bash
xmllint --noout dist/rss.xml dist/rss/*.xml dist/rss/*/*.xml && echo "ALL VALID"
```

Expected: `ALL VALID`。

- [ ] **Step 4: language 對齊**

```bash
for f in dist/rss.xml dist/rss/zh.xml dist/rss/tech.xml dist/rss/life.xml dist/rss/zh/tech.xml dist/rss/zh/life.xml; do echo -n "$f: "; grep -oE '<language>[^<]+</language>' "$f"; done
echo "--- ja ---"
for f in dist/rss/ja.xml dist/rss/ja/tech.xml dist/rss/ja/life.xml; do echo -n "$f: "; grep -oE '<language>[^<]+</language>' "$f"; done
echo "--- en ---"
for f in dist/rss/en.xml dist/rss/en/tech.xml dist/rss/en/life.xml; do echo -n "$f: "; grep -oE '<language>[^<]+</language>' "$f"; done
```

Expected: zh 群組全部 `zh-Hant`、ja 群組全部 `ja`、en 群組全部 `en`。**任何一筆顯示 `zh-tw` 即驗收失敗。**

- [ ] **Step 5: 不混雜（抽樣 ja feed link 全為 /ja/）**

```bash
grep -oE '<link>[^<]+</link>' dist/rss/ja.xml | grep -v 'yurenju.blog</link>' | grep -oE '/[a-z]{2}/posts' | sort -u
```

Expected: 只看到 `/ja/posts`，不可有 `/zh/posts` 或 `/en/posts`。

```bash
grep -oE '<link>[^<]+</link>' dist/rss/en.xml | grep -v 'yurenju.blog</link>' | grep -oE '/[a-z]{2}/posts' | sort -u
```

Expected: 只 `/en/posts`。

```bash
# zh feed 應只有 /zh/posts
grep -oE '<link>[^<]+</link>' dist/rss/zh.xml | grep -v 'yurenju.blog</link>' | grep -oE '/[a-z]{2}/posts' | sort -u
```

Expected: 只 `/zh/posts`（包括 zh 也帶 prefix）。

- [ ] **Step 6: legacy alias 內容對齊**

```bash
diff <(grep -v 'atom:link' dist/rss.xml) <(grep -v 'atom:link' dist/rss/zh.xml) | wc -l
diff <(grep -v 'atom:link' dist/rss/tech.xml) <(grep -v 'atom:link' dist/rss/zh/tech.xml) | wc -l
diff <(grep -v 'atom:link' dist/rss/life.xml) <(grep -v 'atom:link' dist/rss/zh/life.xml) | wc -l
```

Expected: 三筆都 `0`。

- [ ] **Step 7: Item 數量與 archives 排除**

```bash
for f in dist/rss/zh.xml dist/rss/ja.xml dist/rss/en.xml dist/rss/zh/tech.xml dist/rss/zh/life.xml dist/rss/ja/tech.xml dist/rss/ja/life.xml dist/rss/en/tech.xml dist/rss/en/life.xml; do
  c=$(grep -c '<item>' "$f")
  echo "$f: $c items"
done
```

Expected: zh feed 全 = 20，ja feed all 與 tech ≤ 30、life = 0，en 同 ja。

```bash
# Sanity: 沒任何 archives 文章 — 抽樣 link 不含明顯 archive 年份
grep -oE 'href="[^"]+/posts/[^"]+"' dist/rss/zh.xml | grep -E '_19[89][0-9]|_200[0-9]|_201[0-9]' | head
```

Expected: 空（最新 20 篇都是 2020+）。

- [ ] **Step 8: 內容是 HTML（不是 raw markdown）**

```bash
# Sample item description from zh feed should contain HTML tags
grep -oE '<description><\!\[CDATA\[[^]]{0,200}' dist/rss/zh.xml | head -1
```

Expected: 看到 `<p>` 或 `<h` 等 HTML tag，不是純文字 markdown 標記如 `##` 或 `[link](...)`. 

```bash
# Image rendering check — sample one item with image, look for /_astro/ in CDATA content
grep -oE '_astro/[^"]+' dist/rss/zh.xml | head -3
```

Expected: 至少有幾筆 `_astro/...webp` hashed 圖片 URL，證明 image pipeline 已套用。

- [ ] **Step 9: feedvalidator.org 抽樣**

手動把 `dist/rss/zh.xml`、`dist/rss/ja.xml`、`dist/rss/en.xml` 內容貼到 https://www.feedvalidator.org/check.cgi 或上傳，三隻都應 valid（warning 可接受，error 不行）。

如果 build 機器不能上網，跳過此步驟，紀錄 `xmllint` 結果作為 baseline。

- [ ] **Step 10: 跑全部測試**

```bash
npx vitest run
```

Expected: 62 PASS。

- [ ] **Step 11: 更新 roadmap**

修改 `docs/research/2026-04-29-astro-migration-roadmap.md`：

把第 3 行的狀態改為：
```
**狀態：** Phase 0（POC）、Phase 1a、Phase 1b、Phase 2、Phase 3 已完成並 merge 進 main。後續 phase 待執行。
```

Phase 3 章節標題改為：
```
### Phase 3 — RSS ✅ 已完成（YYYY-MM-DD）
```
（YYYY-MM-DD 用實際完成日期）

在「目標」段落上方插入完成備忘段落：

```markdown
**完成 commits：** 起點到終點 SHA。spec：`docs/superpowers/specs/2026-05-02-phase-3-rss-design.md`，plan：`docs/superpowers/plans/2026-05-02-phase-3-rss.md`。

**完成備忘：**
- 改用 `@astrojs/rss` 取代 Next.js 的 `feed` 套件。Item 內容用 Astro Container API（`experimental_AstroContainer.renderToString(Content)`）渲染，與頁面 HTML 一致，圖片自動套用 image pipeline 的 `/_astro/` hashed URL。
- 修掉 Next.js prod 三個 RSS bug：(1) `<language>` 全部硬寫 `zh-tw`；(2) 無 prefix 的 4 隻 feed 三 locale 文章混雜每篇出現 3 次；(3) channel meta 與 item 語言不一致。
- 12 隻 feed 結構：3 個 `/rss/{zh,ja,en}.xml`（per-locale all）+ 6 個 `/rss/{zh,ja,en}/{tech,life}.xml`（per-locale per-category）+ 3 個 legacy alias `/rss.xml`、`/rss/{tech,life}.xml`（內容對齊 zh）。
- shorts category 已不存在於 schema，spec 確認後正式不產生 shorts feed。
- 11 個新 unit test：`channelMeta` 4 + `buildFeedItems` 7。總計 62 個 vitest 測試全綠。
- Build 時間：[實際數字] s（Phase 2 baseline 32s）。20 篇全文 × 12 feeds = 240 次 Container API 渲染呼叫，但因 zh-only 那部分 12 feeds 共用內文 cache 後僅 60 篇 unique entry。
```

填上實際 commit SHA 範圍與完成日期。

- [ ] **Step 12: Commit**

```bash
git add docs/research/2026-04-29-astro-migration-roadmap.md
git commit -m "docs: mark Phase 3 complete in migration roadmap"
```

---

## 自我檢查（plan author）

**Spec 涵蓋檢查：**

| Spec 項目 | 對應 task |
|---|---|
| 12 個 RSS 檔案建出來 | Task 6, 7, 8, 9（個別建立）+ Task 10（驗收所有檔存在） |
| `<language>` 對應 locale | Task 4（channelMeta 邏輯）、Task 6/7/8/9（endpoint customData）、Task 10 step 4（驗收） |
| 不混雜 | Task 5（buildFeedItems 用 caller 提供的 already-filtered posts）+ Task 6/7（用 getActivePosts/getPostsByCategory 已 locale-filtered）+ Task 10 step 5（驗收） |
| Item 限 20 篇全文 | Task 5（buildFeedItems 切 ITEM_LIMIT）+ Task 10 step 7（驗收） |
| 排除 archives | Task 6/7/8/9 用 Phase 2 的 `getActivePosts(locale)` / `getPostsByCategory(category, locale)`，已自動排除 archives + Task 10 step 7（驗收） |
| 排序新到舊 | Phase 2 的 `getAllPosts()` 已排序；endpoint 直接傳遞 |
| Legacy alias 對齊 | Task 8/9 + Task 10 step 6（驗收 diff = 0） |
| feedvalidator.org 抽樣 | Task 10 step 9 |
| Build 時間 < +5s | Task 10 step 1 |
| Item 全文 HTML | Task 5（buildFeedItems content 接 string）+ Task 6/7/8/9（Container API 渲染）+ Task 10 step 8（驗收 `_astro/` hashed URL 出現） |
| markdown-it 路線 spike | Task 2（spike Container API；spec 把 markdown-it 列為首選但這個 plan 直接押 Container API，因為 spec 已記錄 markdown-it 對 hashed image URL 的已知限制） |
| `customData` 處理 `<language>` tag | Task 6/7/8/9（每個 endpoint 都帶 customData） |

**偏差說明：** spec 「Item 全文渲染策略」首選 markdown-it、備案 Container API。本 plan 直接押 Container API，因 spec 自身列舉 markdown-it 的兩個已知限制（hashed image URL 拿不到、`![[]]` 語法漏出）對我們的 corpus 都實際存在。Task 2 spike 的目標從「決定路線」收斂為「驗證 Container API 對含圖文章可行」。如 spike fail 才回頭。這個收斂提升 plan 確定性。

**Placeholder 掃描：** 無 TBD/TODO；每一步都有可執行指令；commit 訊息寫死；測試案例完整。

**型別/簽章一致性：**
- `channelMeta(locale, kind: FeedKind): ChannelMeta` 在 Task 4 定義、Task 6/7/8/9 呼叫 ✓
- `buildFeedItems(posts, renderHtml, locale): Promise<RSSFeedItem[]>` 在 Task 5 定義、Task 6/7/8/9 呼叫 ✓
- `FeedKind` 在 Task 4 定義、Task 7/9 import ✓
- Phase 2 的 `getActivePosts(locale)`、`getPostsByCategory(category, locale)` 簽章 Task 6/7/8/9 一致 ✓

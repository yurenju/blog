# Astro POC 實作計畫

> **給執行的 agent：** 必要 sub-skill：使用 superpowers:subagent-driven-development（推薦）或 superpowers:executing-plans 一個任務一個任務地實作。步驟使用 checkbox（`- [ ]`）語法追蹤進度。

**目標：** 在 `astro/` 子目錄建立 Astro 6.1 POC，與現有 Next.js 專案並存，能渲染 1400+ 篇繁體中文 markdown 文章於 `/zh/*` 路徑下。

**架構：** Astro static output、Content Collections 用 `glob()` loader 指向父層 `public/posts/`、Astro Fonts API 載入 Noto Sans/Serif TC、Astro scoped styles + 一份 global stylesheet、路由結構 `[locale]/...` 但只填 `zh`（ja/en 之後再做）。不處理圖片、不做深色模式、不做 RSS — 嚴格按照 `docs/superpowers/specs/2026-04-29-astro-poc-design.md` 的 POC 範圍。

**技術選型：** Astro 6.x、TypeScript、Astro Content Collections、Astro Fonts API（Google provider）、Node 24 LTS、npm。

---

## 檔案結構

所有路徑相對於 repo 根目錄，新增檔案全部在 `astro/` 之下。

```
astro/
├── package.json                              # Astro deps
├── tsconfig.json                             # 繼承 astro/tsconfigs/strict
├── astro.config.ts                           # static output、fonts、i18n 預留
├── .gitignore                                # node_modules、dist、.astro
├── src/
│   ├── env.d.ts                              # Astro 型別 reference
│   ├── content.config.ts                     # Posts collection + zod schema
│   ├── lib/
│   │   └── posts.ts                          # PostMeta 型別 + 衍生 helpers
│   ├── styles/
│   │   └── global.css                        # reset + prose + 字型套用
│   ├── layouts/
│   │   ├── BaseLayout.astro                  # html shell + Font preload
│   │   └── PostLayout.astro                  # 文章頁外層
│   ├── components/
│   │   ├── Header.astro                      # 站台導覽
│   │   ├── Footer.astro                      # byline / copyright
│   │   ├── PostList.astro                    # 按年份分組列表（共用）
│   │   └── PostMeta.astro                    # 日期 + 分類顯示
│   └── pages/
│       ├── index.astro                       # 根目錄 → /zh redirect（meta refresh）
│       └── [locale]/
│           ├── index.astro                   # locale 首頁：最新非 archived 文章
│           ├── tech.astro                    # category=tech，非 archived
│           ├── life.astro                    # category=life，非 archived
│           ├── archives.astro                # 只顯示 archived
│           ├── about.astro                   # 佔位
│           ├── subscription.astro            # 佔位
│           └── posts/
│               └── [slug].astro              # 單篇文章頁
```

每個檔案職責單一：collection 設定定義 schema、`lib/posts.ts` 處理衍生與篩選、layouts 處理外層 shell、components 純渲染單元、pages 把上述串起來。

---

## 任務 1：建立 branch 並初始化 Astro 專案

**檔案：**
- 新增：`astro/package.json`
- 新增：`astro/.gitignore`
- 新增：`astro/tsconfig.json`
- 新增：`astro/src/env.d.ts`
- 新增：`astro/astro.config.ts`

- [ ] **步驟 1：建立並切到 feature branch**

```bash
git checkout main
git checkout -b astro-poc-v2
```

- [ ] **步驟 2：建立 `astro/package.json`**

```json
{
  "name": "blog-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^6.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.0"
  }
}
```

- [ ] **步驟 3：建立 `astro/.gitignore`**

```
node_modules
dist
.astro
.env
.env.production
.DS_Store
```

- [ ] **步驟 4：建立 `astro/tsconfig.json`**

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

- [ ] **步驟 5：建立 `astro/src/env.d.ts`**

```ts
/// <reference path="../.astro/types.d.ts" />
```

- [ ] **步驟 6：建立 `astro/astro.config.ts`**

```ts
import { defineConfig, fontProviders } from 'astro/config';

export default defineConfig({
  output: 'static',
  site: 'https://yurenju.blog',
  trailingSlash: 'ignore',
  fonts: [
    {
      provider: fontProviders.google(),
      name: 'Noto Sans TC',
      cssVariable: '--font-sans',
      weights: [400, 500, 700],
    },
    {
      provider: fontProviders.google(),
      name: 'Noto Serif TC',
      cssVariable: '--font-serif',
      weights: [400, 700],
    },
  ],
});
```

- [ ] **步驟 7：安裝相依套件**

執行：`cd astro && npm install`
預期：安裝完成無錯誤，`astro/node_modules/` 出現。

- [ ] **步驟 8：驗證 dev server 能啟動**

執行：`cd astro && npm run dev`（背景執行；確認啟動後即停止）
預期：console 顯示 `astro v6.x` 與 local URL。此時可能還沒有頁面（404）—正常。

- [ ] **步驟 9：commit**

```bash
git add astro/package.json astro/package-lock.json astro/.gitignore astro/tsconfig.json astro/src/env.d.ts astro/astro.config.ts
git commit -m "feat(astro): scaffold Astro 6 POC project with fonts API"
```

---

## 任務 2：Content Collection 與文章 helpers

**檔案：**
- 新增：`astro/src/content.config.ts`
- 新增：`astro/src/lib/posts.ts`

- [ ] **步驟 1：建立 `astro/src/content.config.ts`**

```ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const posts = defineCollection({
  loader: glob({
    pattern: '**/*.md',
    base: '../public/posts',
  }),
  schema: z.object({
    title: z.string(),
    date: z.coerce.date(),
    category: z.enum(['tech', 'life']).default('tech'),
    description: z.string().optional(),
    cover: z.string().optional(),
  }),
});

export const collections = { posts };
```

- [ ] **步驟 2：建立 `astro/src/lib/posts.ts`**

```ts
import { getCollection, type CollectionEntry } from 'astro:content';

export type PostEntry = CollectionEntry<'posts'>;

export interface PostMeta {
  entry: PostEntry;
  slug: string;
  group: string;
  archived: boolean;
  category: 'tech' | 'life';
  title: string;
  date: Date;
  description?: string;
}

function toMeta(entry: PostEntry): PostMeta {
  // entry.id looks like "2024/2024-01-01_title/index" or "archives/foo/index"
  const segments = entry.id.split('/');
  const group = segments[0] ?? '';
  const slug = segments[1] ?? entry.id;
  return {
    entry,
    slug,
    group,
    archived: group === 'archives',
    category: entry.data.category,
    title: entry.data.title,
    date: entry.data.date,
    description: entry.data.description,
  };
}

export async function getAllPosts(): Promise<PostMeta[]> {
  const entries = await getCollection('posts');
  return entries
    .map(toMeta)
    .sort((a, b) => b.date.getTime() - a.date.getTime());
}

export async function getActivePosts(): Promise<PostMeta[]> {
  return (await getAllPosts()).filter((p) => !p.archived);
}

export async function getPostsByCategory(
  category: 'tech' | 'life',
): Promise<PostMeta[]> {
  return (await getActivePosts()).filter((p) => p.category === category);
}

export async function getArchivedPosts(): Promise<PostMeta[]> {
  return (await getAllPosts()).filter((p) => p.archived);
}

export function groupByYear(posts: PostMeta[]): Map<number, PostMeta[]> {
  const map = new Map<number, PostMeta[]>();
  for (const post of posts) {
    const year = post.date.getFullYear();
    const list = map.get(year) ?? [];
    list.push(post);
    map.set(year, list);
  }
  return map;
}

export async function getPostBySlug(slug: string): Promise<PostMeta | null> {
  const all = await getAllPosts();
  return all.find((p) => p.slug === slug) ?? null;
}
```

- [ ] **步驟 3：同步 content 型別並驗證 schema 解析**

執行：`cd astro && npx astro sync`
預期：完成無錯誤。若任一篇文章 schema 驗證失敗，錯誤訊息會點出檔名 — 應該放寬 `content.config.ts` 的 schema（例如改用 `passthrough()` 或把欄位設為 `.optional()`），**不要去改 markdown**。

- [ ] **步驟 4：型別檢查**

執行：`cd astro && npm run check`
預期：0 errors。（unused export 警告 OK，後續任務會用到。）

- [ ] **步驟 5：commit**

```bash
git add astro/src/content.config.ts astro/src/lib/posts.ts
git commit -m "feat(astro): define posts collection and metadata helpers"
```

---

## 任務 3：全域樣式與 BaseLayout

**檔案：**
- 新增：`astro/src/styles/global.css`
- 新增：`astro/src/layouts/BaseLayout.astro`

- [ ] **步驟 1：建立 `astro/src/styles/global.css`**

```css
*, *::before, *::after { box-sizing: border-box; }
* { margin: 0; }

html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-sans), system-ui, -apple-system, sans-serif;
  line-height: 2;
  letter-spacing: 0.05em;
  color: #1a1a1a;
  background: #fafafa;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-serif), 'Times New Roman', serif;
  line-height: 1.4;
  letter-spacing: 0;
  font-weight: 700;
}

a { color: inherit; text-decoration: none; }
a:hover { text-decoration: underline; text-underline-offset: 4px; }

img, picture, svg { display: block; max-width: 100%; }

.container {
  max-width: 48rem;
  margin: 0 auto;
  padding: 1.5rem 1rem;
}

/* Markdown prose */
.prose h1 { font-size: 2rem; margin: 2rem 0 1rem; }
.prose h2 { font-size: 1.5rem; margin: 2rem 0 1rem; }
.prose h3 { font-size: 1.25rem; margin: 1.5rem 0 0.75rem; }
.prose p { margin: 1rem 0; }
.prose ul, .prose ol { margin: 1rem 0; padding-left: 1.5rem; }
.prose li { margin: 0.25rem 0; }
.prose blockquote {
  border-left: 4px solid #ccc;
  padding-left: 1rem;
  color: #555;
  margin: 1rem 0;
}
.prose code {
  font-family: ui-monospace, 'SFMono-Regular', Menlo, monospace;
  font-size: 0.9em;
  background: #f0f0f0;
  padding: 0.1em 0.3em;
  border-radius: 3px;
}
.prose pre {
  font-family: ui-monospace, 'SFMono-Regular', Menlo, monospace;
  padding: 1rem;
  border-radius: 6px;
  overflow-x: auto;
  margin: 1rem 0;
  line-height: 1.6;
  letter-spacing: 0;
}
.prose pre code {
  background: transparent;
  padding: 0;
  font-size: 0.875rem;
}
.prose a { color: #0366d6; text-decoration: underline; text-underline-offset: 3px; }
.prose hr { border: 0; border-top: 1px solid #ddd; margin: 2rem 0; }
.prose table { border-collapse: collapse; margin: 1rem 0; width: 100%; }
.prose th, .prose td { border: 1px solid #ddd; padding: 0.5rem 0.75rem; }
.prose th { background: #f5f5f5; font-weight: 600; }
```

- [ ] **步驟 2：建立 `astro/src/layouts/BaseLayout.astro`**

```astro
---
import { Font } from 'astro:assets';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  lang?: string;
}

const { title, description, lang = 'zh-Hant-TW' } = Astro.props;
const siteDescription =
  description ??
  '撰寫就是一種思考方式，發佈與分享只是副產品，而真正的意義是在自我的知識脈絡裡面有了歸屬與連結。';
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={siteDescription} />
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <Font cssVariable="--font-sans" preload />
    <Font cssVariable="--font-serif" preload />
  </head>
  <body>
    <slot />
  </body>
</html>
```

- [ ] **步驟 3：commit**

```bash
git add astro/src/styles/global.css astro/src/layouts/BaseLayout.astro
git commit -m "feat(astro): add global styles and BaseLayout with Fonts API"
```

---

## 任務 4：共用 components（Header、Footer、PostList、PostMeta）

**檔案：**
- 新增：`astro/src/components/Header.astro`
- 新增：`astro/src/components/Footer.astro`
- 新增：`astro/src/components/PostMeta.astro`
- 新增：`astro/src/components/PostList.astro`

- [ ] **步驟 1：建立 `astro/src/components/Header.astro`**

```astro
---
interface Props {
  locale: string;
}
const { locale } = Astro.props;
const base = `/${locale}`;
---
<header class="site-header">
  <div class="inner">
    <a href={base} class="brand">Yuren's Blog</a>
    <nav>
      <a href={`${base}/tech`}>Tech</a>
      <a href={`${base}/life`}>Life</a>
      <a href={`${base}/archives`}>Archives</a>
      <a href={`${base}/about`}>About</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid #e5e5e5;
  }
  .inner {
    max-width: 48rem;
    margin: 0 auto;
    padding: 1rem;
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .brand {
    font-family: var(--font-serif), serif;
    font-size: 1.25rem;
    font-weight: 700;
  }
  nav {
    display: flex;
    gap: 1.25rem;
    font-size: 0.95rem;
  }
</style>
```

- [ ] **步驟 2：建立 `astro/src/components/Footer.astro`**

```astro
---
const year = new Date().getFullYear();
---
<footer class="site-footer">
  <div class="inner">
    <p>© {year} Yuren Ju</p>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid #e5e5e5;
    margin-top: 4rem;
  }
  .inner {
    max-width: 48rem;
    margin: 0 auto;
    padding: 1.5rem 1rem;
    font-size: 0.875rem;
    color: #666;
    text-align: center;
  }
</style>
```

- [ ] **步驟 3：建立 `astro/src/components/PostMeta.astro`**

```astro
---
interface Props {
  date: Date;
  category?: 'tech' | 'life';
}
const { date, category } = Astro.props;
const formatted = date.toISOString().slice(0, 10);
---
<p class="meta">
  <time datetime={date.toISOString()}>{formatted}</time>
  {category && <span class="dot">·</span>}
  {category && <span class="cat">{category}</span>}
</p>

<style>
  .meta {
    font-family: var(--font-serif), serif;
    font-size: 0.875rem;
    color: #666;
    letter-spacing: 0;
  }
  .dot { margin: 0 0.5rem; }
  .cat { text-transform: uppercase; font-size: 0.8rem; }
</style>
```

- [ ] **步驟 4：建立 `astro/src/components/PostList.astro`**

```astro
---
import type { PostMeta } from '../lib/posts';
import { groupByYear } from '../lib/posts';

interface Props {
  locale: string;
  posts: PostMeta[];
}
const { locale, posts } = Astro.props;
const grouped = groupByYear(posts);
const years = [...grouped.keys()].sort((a, b) => b - a);
---
<div class="post-list">
  {years.map((year) => (
    <section class="year">
      <h2>{year}</h2>
      <ul>
        {grouped.get(year)!.map((post) => (
          <li>
            <a href={`/${locale}/posts/${post.slug}`}>
              <span class="date">{post.date.toISOString().slice(5, 10)}</span>
              <span class="title">{post.title}</span>
            </a>
          </li>
        ))}
      </ul>
    </section>
  ))}
</div>

<style>
  .year { margin-bottom: 3rem; }
  .year h2 {
    font-family: var(--font-serif), serif;
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
    color: #444;
  }
  ul { list-style: none; padding: 0; }
  li { margin: 0.75rem 0; }
  li a {
    display: flex;
    gap: 1rem;
    align-items: baseline;
  }
  .date {
    font-family: var(--font-serif), serif;
    font-size: 0.875rem;
    color: #888;
    min-width: 4rem;
    letter-spacing: 0;
  }
  .title { flex: 1; }
  li a:hover .title { text-decoration: underline; text-underline-offset: 4px; }
</style>
```

- [ ] **步驟 5：型別檢查**

執行：`cd astro && npm run check`
預期：0 errors。

- [ ] **步驟 6：commit**

```bash
git add astro/src/components/
git commit -m "feat(astro): add Header, Footer, PostMeta, PostList components"
```

---

## 任務 5：`/zh` 首頁

**檔案：**
- 新增：`astro/src/pages/[locale]/index.astro`

- [ ] **步驟 1：建立 `astro/src/pages/[locale]/index.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getActivePosts } from '../../lib/posts';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
const posts = await getActivePosts();
---
<BaseLayout title="Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1 class="page-title">最新文章</h1>
    <PostList locale={locale!} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title {
    font-size: 2.25rem;
    margin-bottom: 2rem;
  }
</style>
```

- [ ] **步驟 2：build 驗證**

執行：`cd astro && npm run build`
預期：build 成功，輸出顯示 `[locale]/index.html` 對 `zh` 產出。沒有 schema 錯誤。

- [ ] **步驟 3：用 dev server 手動驗證**

執行：`cd astro && npm run dev`
瀏覽器開 `http://localhost:4321/zh`
預期：頁面載入，顯示站名、導覽列、按年份分組的文章列表。確認後停止 dev server。

- [ ] **步驟 4：commit**

```bash
git add astro/src/pages/[locale]/index.astro
git commit -m "feat(astro): add /zh home page with year-grouped post list"
```

---

## 任務 6：分類頁與封存頁

**檔案：**
- 新增：`astro/src/pages/[locale]/tech.astro`
- 新增：`astro/src/pages/[locale]/life.astro`
- 新增：`astro/src/pages/[locale]/archives.astro`

- [ ] **步驟 1：建立 `astro/src/pages/[locale]/tech.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getPostsByCategory } from '../../lib/posts';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
const posts = await getPostsByCategory('tech');
---
<BaseLayout title="Tech · Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1 class="page-title">Tech</h1>
    <PostList locale={locale!} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **步驟 2：建立 `astro/src/pages/[locale]/life.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getPostsByCategory } from '../../lib/posts';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
const posts = await getPostsByCategory('life');
---
<BaseLayout title="Life · Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1 class="page-title">Life</h1>
    <PostList locale={locale!} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **步驟 3：建立 `astro/src/pages/[locale]/archives.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostList from '../../components/PostList.astro';
import { getArchivedPosts } from '../../lib/posts';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
const posts = await getArchivedPosts();
---
<BaseLayout title="Archives · Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1 class="page-title">Archives</h1>
    <PostList locale={locale!} posts={posts} />
  </main>
  <Footer />
</BaseLayout>

<style>
  .page-title { font-size: 2.25rem; margin-bottom: 2rem; }
</style>
```

- [ ] **步驟 4：用 dev server 驗證**

執行：`cd astro && npm run dev`
瀏覽器分別開：`/zh/tech`、`/zh/life`、`/zh/archives`
預期：每頁顯示對應的子集（tech / life / 僅 archived）。確認後停止 dev server。

- [ ] **步驟 5：commit**

```bash
git add astro/src/pages/[locale]/tech.astro astro/src/pages/[locale]/life.astro astro/src/pages/[locale]/archives.astro
git commit -m "feat(astro): add tech/life/archives category pages"
```

---

## 任務 7：文章詳細頁

**檔案：**
- 新增：`astro/src/layouts/PostLayout.astro`
- 新增：`astro/src/pages/[locale]/posts/[slug].astro`

- [ ] **步驟 1：建立 `astro/src/layouts/PostLayout.astro`**

```astro
---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PostMeta from '../components/PostMeta.astro';
import type { PostMeta as PostMetaType } from '../lib/posts';

interface Props {
  locale: string;
  post: PostMetaType;
}

const { locale, post } = Astro.props;
const pageTitle = `${post.title} · Yuren's Blog`;
---
<BaseLayout title={pageTitle} description={post.description}>
  <Header locale={locale} />
  <main class="container">
    <article>
      <header class="post-head">
        <h1>{post.title}</h1>
        <PostMeta date={post.date} category={post.category} />
      </header>
      <div class="prose">
        <slot />
      </div>
    </article>
  </main>
  <Footer />
</BaseLayout>

<style>
  .post-head {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid #eee;
  }
  .post-head h1 {
    font-size: 2.25rem;
    margin-bottom: 0.75rem;
  }
</style>
```

- [ ] **步驟 2：建立 `astro/src/pages/[locale]/posts/[slug].astro`**

```astro
---
import PostLayout from '../../../layouts/PostLayout.astro';
import { getAllPosts } from '../../../lib/posts';
import { render } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getAllPosts();
  const locales = ['zh'];
  return locales.flatMap((locale) =>
    posts.map((post) => ({
      params: { locale, slug: post.slug },
      props: { post },
    })),
  );
}

const { locale } = Astro.params;
const { post } = Astro.props;
const { Content } = await render(post.entry);
---
<PostLayout locale={locale!} post={post}>
  <Content />
</PostLayout>
```

- [ ] **步驟 3：build 驗證**

執行：`cd astro && npm run build`
預期：build 完成，輸出顯示產出數千頁文章於 `dist/zh/posts/<slug>/index.html`。記下 build 時間作為基準。

- [ ] **步驟 4：抽樣檢查文章渲染**

執行：`cd astro && npm run preview`（背景）
瀏覽器打開某篇實際存在的文章 URL，例如 `http://localhost:4321/zh/posts/<some-real-slug>`
預期：顯示文章標題、日期、分類，markdown 內容渲染正確。code block 有 Shiki 語法高亮（Astro 預設 `github-dark` theme）。內嵌 `<img>` 會破圖 — 依 spec 屬預期行為。確認後停止 preview server。

- [ ] **步驟 5：commit**

```bash
git add astro/src/layouts/PostLayout.astro astro/src/pages/[locale]/posts/[slug].astro
git commit -m "feat(astro): add post detail page with dynamic [slug] route"
```

---

## 任務 8：About 與 Subscription 佔位頁

**檔案：**
- 新增：`astro/src/pages/[locale]/about.astro`
- 新增：`astro/src/pages/[locale]/subscription.astro`

- [ ] **步驟 1：建立 `astro/src/pages/[locale]/about.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
---
<BaseLayout title="About · Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1>About</h1>
    <p>About 頁面內容稍後遷移。</p>
  </main>
  <Footer />
</BaseLayout>

<style>
  h1 { font-size: 2.25rem; margin-bottom: 1.5rem; }
</style>
```

- [ ] **步驟 2：建立 `astro/src/pages/[locale]/subscription.astro`**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}

const { locale } = Astro.params;
---
<BaseLayout title="Subscription · Yuren's Blog">
  <Header locale={locale!} />
  <main class="container">
    <h1>訂閱</h1>
    <p>訂閱頁面內容稍後遷移。</p>
  </main>
  <Footer />
</BaseLayout>

<style>
  h1 { font-size: 2.25rem; margin-bottom: 1.5rem; }
</style>
```

- [ ] **步驟 3：commit**

```bash
git add astro/src/pages/[locale]/about.astro astro/src/pages/[locale]/subscription.astro
git commit -m "feat(astro): add about and subscription placeholder pages"
```

---

## 任務 9：根目錄 redirect 與最終驗收

**檔案：**
- 新增：`astro/src/pages/index.astro`

- [ ] **步驟 1：建立 `astro/src/pages/index.astro`**

```astro
---
---
<!doctype html>
<html lang="zh-Hant-TW">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content="0; url=/zh/" />
    <link rel="canonical" href="/zh/" />
    <title>Yuren's Blog</title>
  </head>
  <body>
    <a href="/zh/">/zh/</a>
  </body>
</html>
```

- [ ] **步驟 2：完整乾淨 build**

執行：`cd astro && rm -rf dist .astro && npm run build`
預期：
- build 完成無錯誤。
- 輸出顯示總頁數：應該是 1400+ 文章頁加上 7 個固定頁（index、/zh、tech、life、archives、about、subscription）。
- 記下 build 總時間。

- [ ] **步驟 3：驗證輸出結構**

執行：`cd astro && ls dist && ls dist/zh && ls dist/zh/posts | wc -l`
預期：
- `dist/index.html` 存在（根目錄 redirect）
- `dist/zh/` 含 `index.html`、`tech/`、`life/`、`archives/`、`about/`、`subscription/`、`posts/`
- `dist/zh/posts/` 含 1400+ slug 子目錄。

- [ ] **步驟 4：透過 preview 跑驗收標準**

執行：`cd astro && npm run preview`（背景）
逐項驗證 spec 的驗收標準：
1. `http://localhost:4321/` → redirect 到 `/zh/`
2. `/zh/` 顯示按年份分組的文章列表
3. `/zh/tech`、`/zh/life`、`/zh/archives` 顯示正確子集
4. `/zh/about`、`/zh/subscription` 顯示佔位內容
5. 隨機抽一篇 `/zh/posts/<slug>` 渲染標題、日期、分類、markdown 內文，且 code block 有 Shiki 高亮
6. 字型正確載入：內文 Noto Sans TC、標題 Noto Serif TC（DevTools Network 確認字型來自同網域，不是 Google CDN）

確認後停止 preview server。

- [ ] **步驟 5：commit**

```bash
git add astro/src/pages/index.astro
git commit -m "feat(astro): add root redirect to /zh and complete POC"
```

- [ ] **步驟 6：推 branch**

```bash
git push -u origin astro-poc-v2
```

---

## 給實作者的備註

- **`Astro.params.locale` 的型別是 `string | undefined`**，因為 `[locale]` 是動態參數。在 `getStaticPaths` 驅動的頁面內部我們已知它有值，因此使用 `!` non-null assertion 是有意為之、安全的。
- **若 `astro sync` 對某篇文章失敗**，代表 `content.config.ts` schema 太嚴。檢視該篇 frontmatter，**放寬 schema** 處理（多餘欄位可用 `passthrough()`、改 `.optional()` 等）。**不要改 markdown 檔案**。
- **build 時間是觀察重點** — 在最終 commit message 或回報時記錄下來。POC 主要驗證的就是 Astro 處理 1400+ 篇文章的速度。
- **破圖的 `<img>`** 屬預期行為、不在範圍內，不要嘗試補圖片處理邏輯。
- **不要加 Tailwind、深色模式、RSS、sitemap**，spec 明確排除。
